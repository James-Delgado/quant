# Copy this file to `.env` and fill in your keys. `.env` is gitignored.
# All four providers have free tiers — no payment required for Phase 0.

# Alpaca — free account at https://alpaca.markets (data works on unfunded accounts)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=

# Tiingo — free token at https://www.tiingo.com
TIINGO_API_KEY=

# FRED — free key at https://fredaccount.stlouisfed.org/apikeys
FRED_API_KEY=

# How many years of history to pull on a first-time backfill run.
# Default is 5. Set to 20 to pull full history for Phase 2.5 feature work.
BACKFILL_YEARS=20

# --- Phase 3: text ingestion + sentiment ---

# SEC EDGAR requires your email in the User-Agent header (free, no registration).
# Format: "YourName your@email.com" — see https://www.sec.gov/os/accessing-edgar-data
EDGAR_USER_AGENT=

# FinBERT inference device override. Leave blank for auto (mps > cuda > cpu).
# Options: auto | mps | cuda | cpu
FINBERT_DEVICE=auto
