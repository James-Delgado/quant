# Living priorities list — the agent (or human) entry point for "what's next."
#
# Read order for a fresh agent:
#   1. docs/AGENT_OPERATION.md  (the standard operating procedure — the 11-step
#                                workflow every task runs through; binding)
#   2. docs/PROJECT_ROADMAP.md  (what we're building and why)
#   3. docs/METHODOLOGY.md      (how to do it without p-hacking — 20 binding rules)
#   4. docs/PRIORITIES.yaml     (this file — pick the lowest-`rank` `ready` task)
#   5. The task's `references` and `deliverable` fields
#
# Update this file every time a task moves between status values. The file
# is the single source of truth for backlog state.
# Full lifecycle / append / post-task-review protocol lives in the
# `execution_model` section below.

version: 1
last_updated: 2026-06-28

# -----------------------------------------------------------------------------
# Schema
# -----------------------------------------------------------------------------
schema:
  task_status:
    - ready        # all dependencies done; pickable now
    - blocked      # one or more dependencies not done
    - in_progress  # an agent or human has claimed it; only one at a time
    - done         # complete; deliverable exists and tests (if any) pass
    - skipped      # explicitly skipped per a conditional rule in a PRD
  complexity:
    - small        # < 1 day wall-time (drafting a PRD, building a single module + tests)
    - medium       # 1–3 days (executing a milestone end-to-end)
    - large        # > 3 days or > 1 hr compute (full-panel runs, multi-arm experiments)

# -----------------------------------------------------------------------------
# Execution model
# -----------------------------------------------------------------------------
execution_model:
  concurrency: single-threaded
  rationale: |
    Parallel agent commits on overlapping files (most often
    `backtest/harness.py`, `models/*.py`, `features/catalog.yaml`) will
    produce merge conflicts that the harness is not yet built to resolve.
    Single-threaded execution from the top of this list is the safe
    default. Concurrent agent execution via git worktrees is a Phase 5
    sub-feature — see docs/PHASE_5_AGENTS.md.

  task_lifecycle: |
    Completed tasks STAY in this file with `status: done` and
    `completed_at: YYYY-MM-DD`. We do not delete them. Reasons:
      - Dependency graph integrity: `depends_on: [A-LEDGER]` must
        resolve for the lifetime of the file. Deleting `A-LEDGER`
        breaks every forward reference.
      - Project-state vs timeline: this file records the current
        project state; git history records the timeline (who did what,
        when). Mixing the two makes dependency checks impossible.
      - Growth is bounded: at ~1 task/day, the file stays under 100KB
        for years. Archive to docs/historical/PRIORITIES_done.yaml only
        when pressure exists, never before.
    A drift test (`tests/test_priorities.py`, planned alongside
    `A-LEDGER`) enforces: (a) all status values are in the enum,
    (b) all depends_on / blocks references resolve to existing task
    ids, (c) at most one task is `in_progress`, (d) `done` tasks have
    `completed_at`.

  append_protocol: |
    Any agent or human who discovers a new task during work APPENDS it
    to the `tasks` array immediately. Discovered work is captured, not
    dropped on the floor.
      - Choose a unique `id` (kebab-uppercase, e.g. `C2-M2-SMOKE`).
      - Set `status: ready` if it has no unmet dependencies, else
        `blocked` with `depends_on` populated.
      - Set `rank` to fit the priority order; renumber neighbours as
        needed.
      - Populate `notes` with a link to the commit, PR, or notebook
        where the gap was discovered. This is the audit trail.
      - Commit the appended task alongside the work that discovered it.
    For Phase 5: Agent R inherits the triage layer (see
    docs/PHASE_5_AGENTS.md §"Workstream 3"); appended tasks pass
    through a `status: proposed` → `triaged` gate before becoming
    `ready`.

  pick_procedure: |
    1. Read this file.
    2. Find the lowest `rank` task with `status: ready`.
    3. Read its `references.primary` and `references.methodology`
       (docs/METHODOLOGY.md is binding for every task).
    4. Set its `status` to `in_progress`, commit this file alone.
    5. Do the work; produce the `deliverable.file`. Land tests
       alongside per METHODOLOGY.md §15.
    6. Post-task review (METHODOLOGY.md §20, MANDATORY before
       flipping to `done`):
         a. Re-read the deliverable for limitations, bugs, unintended
            behaviour changes.
         b. Cross-check against METHODOLOGY.md — particularly §1
            (pre-committed thresholds), §6 (drift contracts), §15
            (tests landed), §17 (E2E notebook if cross-module).
         c. Append any discovered follow-up tasks to this file per
            `append_protocol`.
         d. Note any methodology deviations explicitly in the commit
            message (METHODOLOGY.md §9).
    7. Set its `status` to `done`, populate `completed_at` (ISO date),
       and walk the `blocks` list — any blocked task whose `depends_on`
       is now fully `done` flips to `ready`.
    8. Commit the deliverable + this file together.

# -----------------------------------------------------------------------------
# Projects
# -----------------------------------------------------------------------------
projects:
  A:
    name: Research substrate & methodology
    status: maintain
    description: |
      Harness, catalog, ablations, regime machinery, runner pattern. Built
      through Phase 4A. Future tasks here are pattern improvements
      identified in PHASE_4A_RETROSPECTIVE.md (trial-count ledger, DSR-aware
      gates, etc.).
    roadmap_section: docs/PROJECT_ROADMAP.md#project-a--research-substrate--methodology-done--maintain
  B:
    name: Predictive research (post-4A)
    status: active
    description: |
      Replacement for Phase 4A's research mission. Sub-projects B1
      (target reframing), B2 (OOS attribution), B3 (alternative data),
      B4 (universe shift).
    roadmap_section: docs/PROJECT_ROADMAP.md#project-b--predictive-research-post-4a
  C:
    name: Live execution & deployment infrastructure
    status: active
    description: |
      Built in parallel with Project B. Sub-projects C1 (live data),
      C2 (LEAN local + paper), C3 (sizing + risk), C4 (confidence
      calibration), C5 (monitoring — SUPERSEDED by Project E3; live
      monitoring + paper-vs-backtest reconciliation now ship in the console.
      The C5-PRD task is retained pending a closeout reconciliation rather
      than skipped, to avoid deadlocking C-CLOSE.depends_on).
    roadmap_section: docs/PROJECT_ROADMAP.md#project-c--live-execution--deployment-infrastructure
  D:
    name: Continuous research agents (Phase 5)
    status: gated
    triggers_required:
      - "Project B has produced an ablation matrix with at least one cell that clears its pre-committed gate"
      - "Project B2 OOS attribution method is in code with M3 catalog integration shipped"
    description: |
      Vision spec: docs/PHASE_5_AGENTS.md. Begins only when both triggers
      above are met. Until then, every D-prefixed task in this file is
      `blocked` and unpickable.
  E:
    name: Human Interface & Observability
    status: active
    description: |
      Makes the platform's results, models, and data human-visible. Four
      sub-projects chained E1 -> E2 -> E3/E4, each with different
      dependencies: E1 (Research & Trust Console — buildable now over existing
      artifacts), E2 (Console API — gated on C1), E3 (Live Monitoring — gated
      on E2 + C2/C3; SUPERSEDES C5), E4 (Data & Market Status — gated on
      E2 + C1). Two-layer architecture: a tested Python service layer
      (src/quant/console/) the API later reuses, plus a disposable React/TS
      SPA. No new datastore — reads existing storage/ + features/ artifacts.
      Full PRDs + consensus mockup + rationale live in docs/project-e/
      (DECISIONS.md is the decision log of record). Binds METHODOLOGY rules
      15-21; every sub-project ends with a *-CLOSE end-to-end validation.
    roadmap_section: docs/PROJECT_ROADMAP.md#project-e--human-interface--observability

# -----------------------------------------------------------------------------
# Tasks (ranked; lowest rank = next up)
# -----------------------------------------------------------------------------
tasks:

  # ------ Methodology infrastructure (unblocks everything else) ------

  - id: A-LEDGER
    rank: 1
    title: "Build trial-count ledger (data/ledger.yaml + writer + CI drift test)"
    project: A
    sub_project: methodology-upgrade-12
    status: done
    started_at: 2026-06-17T17:17:12Z
    completed_at: 2026-06-17
    depends_on: []
    blocks: [B1-PRD, B2-PRD, C1-PRD, A-LEDGER-RUNNERS, A-DSR-GATE, A-CLOSE]
    deliverable:
      file: data/ledger.yaml
      schema_doc: docs/METHODOLOGY.md
      tests: tests/test_ledger.py
    references:
      primary: docs/METHODOLOGY.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Schema is in METHODOLOGY.md §"Reference schemas". Append-only YAML.
      Writer can be a tiny library in src/quant/ledger.py (e.g. append_ledger_entry(record)).
      CI test: tests/test_ledger.py asserting (a) schema validity for every existing entry,
      (b) ids unique and monotonically dated, (c) the file is append-only across commits
      (use git history to verify no entries were modified).
      Populate retroactively: add ledger entries for each Phase 4A milestone (M1-M6)
      pointing to the existing artifacts.

  # ------ PRD drafting (parallel-ready once the ledger lands) ------

  - id: B1-PRD
    rank: 2
    title: "Draft B1 (target reframing) PRD"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-23T16:26:35Z
    completed_at: 2026-06-23
    depends_on: [A-LEDGER]
    blocks: [B1-M1, B1-M2, B1-M3, A-METH-NONSHARPE-SYNC]
    deliverable:
      file: .claude/prds/b1-target-reframing.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
      methodology: docs/METHODOLOGY.md
      historical_context: docs/historical/PHASE_4_ADVANCED.md
      verdict_context: docs/PHASE_4A_REPORT.md
    est_complexity: small
    notes: |
      Use /plan-prd workflow. Pre-commit four candidate targets with rationale:
        - 21-day drawdown classification (binary: P(>5% drawdown in next 21 bars))
        - 21-day realized vol prediction (regression on log-vol)
        - 5-day directional (binary up/down)
        - 21-day cumulative direction (binary up/down)
      Each target must declare:
        - materiality threshold (ΔAUC for classification, ΔSharpe-equivalent for regression)
        - significance threshold (bootstrapped 90% CI, paired against ARIMA baseline)
        - DSR threshold (must clear at ledger N at completion time)
      Pre-commit gate function (extension of phase4a_gate_report) in code BEFORE M2 starts.

  - id: B2-PRD
    rank: 3
    title: "Draft B2 (OOS-only attribution method) PRD"
    project: B
    sub_project: B2
    status: done
    started_at: 2026-06-23T16:52:25Z
    completed_at: 2026-06-23
    depends_on: [A-LEDGER]
    blocks: [B2-M1, B2-M2, B2-M3]
    deliverable:
      file: .claude/prds/b2-oos-attribution.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
      methodology: docs/METHODOLOGY.md
      problem_evidence: docs/PHASE_4A_RETROSPECTIVE.md
    est_complexity: small
    notes: |
      Method shortlist (pick 2 of 3 to implement):
        - permutation importance computed on the test fold only
        - per-fold ablation as canonical OOS signal (M3-style, systematized)
        - conformal feature relevance (theory-based, less established in finance)
      Sanity test: each method's ranking, run on the M6 final feature set,
      correlates with M3's per-fold ablation lifts at Spearman ρ > 0.5
      (the inverse of the broken IS signal ρ = -0.074).
      Catalog integration: add `attribution_status` field to catalog.yaml
      with enum {none, ablation_only, oos_permutation, both, agreed}.

  - id: C1-PRD
    rank: 4
    title: "Draft C1 (live data + same-day inference pipeline) PRD"
    project: C
    sub_project: C1
    status: done
    started_at: 2026-06-23T17:30:00Z
    completed_at: 2026-06-23
    depends_on: [A-LEDGER]
    blocks: [C1-M1, C1-M2, C1-M3, C2-PRD]
    deliverable:
      file: .claude/prds/c1-live-data.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
      methodology: docs/METHODOLOGY.md
      existing_ingestors: src/quant/ingest/
    est_complexity: small
    notes: |
      Audit current ingestors (Alpaca, Tiingo, FRED, EDGAR) for same-day freshness.
      Each gets an SLA: "by what time of day can we have today's bar/observation?"
      Define `get_pit_bar(symbol, asof)` returning the most recent point-in-time-correct
      bar; integrate into build_features() such that build_features(asof=today) works.
      No intraday — daily cadence per ratified decision (PROJECT_ROADMAP §8).

  # ------ Project A methodology follow-ups + closeout (discovered during A-LEDGER) ------
  # Ranked ahead of B/C execution because A-DSR-GATE and A-LEDGER-RUNNERS are
  # methodology prerequisites that downstream tasks depend_on (B1-M2 -> A-DSR-GATE,
  # B1-M3 -> A-LEDGER-RUNNERS). A-CLOSE is the Project A closeout (METHODOLOGY §21),
  # gated on all A tasks. depends_on is the hard ordering constraint; rank is the hint.

  - id: A-PRIORITIES-TEST
    rank: 5
    title: "Build tests/test_priorities.py (status enum + dependency-graph + single-in-progress drift test)"
    project: A
    sub_project: methodology-upgrade
    status: done
    started_at: 2026-06-23T18:05:00Z
    completed_at: 2026-06-23
    depends_on: []
    blocks: [A-CLOSE]
    deliverable:
      file: tests/test_priorities.py
    references:
      primary: docs/PRIORITIES.yaml
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Referenced in this file's execution_model (task_lifecycle) and in
      docs/AGENT_OPERATION.md Step 5 as a drift test, but it does not exist yet.
      Enforce: (a) all status values in the enum, (b) all depends_on / blocks
      references resolve to existing ids, (c) at most one task in_progress,
      (d) done tasks have completed_at. Discovered during A-LEDGER (commit of
      src/quant/ledger.py); mirrors the tests/test_ledger.py + tests/test_catalog.py
      drift-test pattern.

  - id: A-LEDGER-RUNNERS
    rank: 6
    title: "Wire append_ledger_entry into the runner pattern (runners write ledger entries automatically)"
    project: A
    sub_project: methodology-upgrade-12
    status: done
    started_at: 2026-06-19T21:16:10Z
    completed_at: 2026-06-19
    depends_on: [A-LEDGER]
    blocks: [B1-M3, A-CLOSE]
    deliverable:
      file: scripts/run_phase4a_arms.py
      tests: tests/test_ledger.py
    references:
      primary: src/quant/ledger.py
      methodology: docs/METHODOLOGY.md
      runner_pattern: scripts/run_phase4a_arms.py
    est_complexity: small
    notes: |
      METHODOLOGY §12 says the ledger is "written by every runner". Today
      data/ledger.yaml is hand-populated; no runner calls
      quant.ledger.append_ledger_entry. Add a small helper that maps a
      runner's metadata.json (config_hash, started_at/finished_at, n_folds,
      verdict) to a LedgerEntry and appends it on completion. Backfill is
      already done (Phase 4A M1-M6); this closes the loop for future B1/B2/C
      runners so the deflation N stays current automatically. Discovered during
      A-LEDGER.

  - id: A-DSR-GATE
    rank: 7
    title: "DSR-aware gate function (deflated Sharpe second stage; N from cumulative_trial_count)"
    project: A
    sub_project: methodology-upgrade-13
    status: done
    started_at: 2026-06-23T20:30:00Z
    completed_at: 2026-06-23
    depends_on: [A-LEDGER]
    blocks: [B1-M2, A-CLOSE]
    deliverable:
      file: src/quant/backtest/regime_metrics.py
      tests: tests/test_regime_metrics.py
    references:
      primary: docs/METHODOLOGY.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      METHODOLOGY §13 ("DSR-aware gates"): gate-pass should require both the
      pre-committed Sharpe threshold AND deflated Sharpe (Bailey & López de
      Prado 2014) > 0, with the deflation N read from
      quant.ledger.cumulative_trial_count(). Extend phase4a_gate_report (or add
      a sibling) so future PRD gates consume N from the ledger rather than
      hand-counting. Discovered during A-LEDGER — the ledger now makes N a code
      value (PHASE_4A_REPORT §7 documents the N≈62 / ~0.71 best-of-N reasoning
      this would formalize).

  - id: A-CLOSE
    rank: 8
    title: "A-CLOSE: Project A closeout — E2E methodology-substrate notebook + closeout report"
    project: A
    sub_project: closeout
    status: blocked
    depends_on: [A-LEDGER, A-PRIORITIES-TEST, A-PRIORITIES-TEST-TS, A-LEDGER-RUNNERS, A-DSR-GATE, A-DSR-LEDGER-SHARPE, A-DSR-DOC-SYNC, A-DSR-WIRE-EMPIRICAL-STD, A-DSR-METH-SCHEMA-SYNC]
    blocks: []
    deliverable:
      notebook: notebooks/12_project_a_closeout.ipynb
      report: docs/PROJECT_A_CLOSEOUT.md
    references:
      primary: docs/METHODOLOGY.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Project-closeout task (METHODOLOGY §21 / AGENT_OPERATION "Project
      closeout"). The notebook must exercise the assembled Project A substrate
      end-to-end across module boundaries: load the feature catalog + ledger,
      append a trial via the runner integration (A-LEDGER-RUNNERS), read N via
      cumulative_trial_count(), evaluate a DSR-aware gate on it (A-DSR-GATE),
      and show the priorities drift test (A-PRIORITIES-TEST) green. The report is
      the one-page definition-of-done. Any integration defect the notebook
      surfaces becomes a new task and keeps A-CLOSE open (AGENT_OPERATION Step 7
      corollary).

  # ------ B1 execution ------

  - id: B1-M1
    rank: 9
    title: "B1-M1: Candidate target catalog (4 targets pre-committed)"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-24T16:16:17Z
    completed_at: 2026-06-24
    depends_on: [B1-PRD]
    blocks: [B1-M2]
    deliverable:
      file: src/quant/features/targets.py
      tests: tests/test_targets.py
    references:
      primary: .claude/prds/b1-target-reframing.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      DESIGN FORK to weigh deliberately (do not default): is targets.py a flat
      module of 4 functions, or a REGISTERED TARGET CATALOG mirroring
      features/catalog.{py,yaml}? For B1's 4 hand-picked targets a flat module
      suffices. But the Phase-5 Research agent (Agent F, ROADMAP §4 Project D:
      "all candidate-generation artifact types ... prediction targets") needs the
      catalog form to add targets SAFELY — a per-target YAML record (metric,
      baseline, horizon, materiality threshold, ablation/gate status) under a
      drift test, i.e. METHODOLOGY §4 (contract-before-consumer) + §6 (drift in
      both directions) applied to targets, exactly as M4 did for features. Decide
      flat-vs-catalog with that future consumer in mind; if flat, leave a note for
      the eventual catalog migration. Captured 2026-06-23 from the B1-PRD review
      discussion (after commit a481a18).

  - id: B1-M2
    rank: 10
    title: "B1-M2: Per-target ablation matrix on 5-symbol × 8-year slice"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-24T17:04:22Z
    completed_at: 2026-06-24
    depends_on: [B1-M1, A-DSR-GATE]
    blocks: [B1-M3]
    deliverable:
      notebook: notebooks/10_b1_target_ablation.ipynb
    references:
      primary: .claude/prds/b1-target-reframing.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      Each of the 4 targets × {ARIMA control, GBM, naive baseline} on slice.
      Verdict via b1_gate_report (src/quant/backtest/regime_metrics.py, shipped
      in B1-M1); it takes per-regime metric values, per-regime significance CIs
      (from statistics.bootstrap_metric_delta_ci, shipped in B1-M1), and a
      pre-computed deflation_passed bool. Borda composite across regimes.

      Captured during B1-M1 post-task review (commit of src/quant/features/
      targets.py) — work that lives in this milestone, surfaced while building
      the gate:
        (a) DEFLATION COMPUTATION. b1_gate_report consumes a deflation_passed
            bool. For the directional targets (deflation="dsr") compute it via
            statistics.deflated_sharpe_ratio (exists). For the non-tradeable
            targets T1 drawdown + T2 vol (deflation="skill_z") there is NO helper
            yet — M2 must add a forecast-skill z-score (z = mean(skill)/se(skill),
            pass if > 0) before scoring those two targets.
        (b) T1 DEFLATION METHOD (§1/§9). The B1 PRD T1 row literally says
            "DSR > 0", but a drawdown PROBABILITY has no return series; B1-M1 set
            TARGET_CATALOG["drawdown_21d"].deflation="skill_z" (the analog the PRD
            prose grants the vol target). Confirm this method BEFORE scoring T1;
            if it changes the gate it requires a new ledger entry, not a mid-
            matrix override.
        (c) INVARIANT-4 ASSERTION (backtest/CLAUDE.md). The new 5- and 21-bar
            horizons exceed Phase 4A's 1-bar label. Assert invariant 4 (test-fold
            length >> label_horizon + embargo) on THIS milestone's real slice
            config; flag rather than silently shrink the training set. B1-M1
            could not assert it (it produces labels, runs no backtest).

  - id: B1-M3
    rank: 11
    title: "B1-M3: Full-panel confirmation of slice-winners (compute-gated)"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-27T18:45:12Z
    completed_at: 2026-06-27
    depends_on: [B1-M2, A-LEDGER-RUNNERS]
    blocks: [B3-PRD, B4-PRD]
    deliverable:
      runner: scripts/run_b1_arms.py
      notebook: notebooks/11_b1_exit_gate.ipynb
      report: docs/B1_REPORT.md
    references:
      primary: .claude/prds/b1-target-reframing.prd.md
      methodology: docs/METHODOLOGY.md
      runner_pattern: scripts/run_phase4a_arms.py
    est_complexity: large

  # ------ B2 execution ------

  - id: B2-M1
    rank: 12
    title: "B2-M1: OOS attribution method shortlist + theory writeup"
    project: B
    sub_project: B2
    status: done
    started_at: 2026-06-27T21:44:46Z
    completed_at: 2026-06-27
    depends_on: [B2-PRD]
    blocks: [B2-M2]
    deliverable:
      file: docs/concepts/oos-attribution.md
    references:
      primary: .claude/prds/b2-oos-attribution.prd.md
    est_complexity: small

  - id: B2-M2
    rank: 13
    title: "B2-M2: Implementation + sanity test against M3 ablation lifts"
    project: B
    sub_project: B2
    status: done
    started_at: 2026-06-27T22:42:21Z
    completed_at: 2026-06-27
    depends_on: [B2-M1]
    blocks: [B2-M3]
    deliverable:
      file: src/quant/backtest/attribution.py
      tests: tests/test_attribution.py
    references:
      primary: .claude/prds/b2-oos-attribution.prd.md
    est_complexity: medium

  - id: B2-M3
    rank: 14
    title: "B2-M3: Catalog integration (attribution_status field)"
    project: B
    sub_project: B2
    status: done
    started_at: 2026-06-27T23:30:00Z
    completed_at: 2026-06-27
    depends_on: [B2-M2]
    blocks: []
    deliverable:
      file: src/quant/features/catalog.yaml
      schema_update: src/quant/features/catalog.py
      tests: tests/test_catalog.py
    references:
      primary: .claude/prds/b2-oos-attribution.prd.md
    est_complexity: small

  # ------ C1 execution ------

  - id: C1-M1
    rank: 15
    title: "C1-M1: Same-day data SLA audit per ingestor"
    project: C
    sub_project: C1
    status: done
    started_at: 2026-06-27T23:44:11Z
    completed_at: 2026-06-27
    depends_on: [C1-PRD]
    blocks: [C1-M2]
    deliverable:
      file: docs/concepts/data-freshness-slas.md
    references:
      primary: .claude/prds/c1-live-data.prd.md
      ingestors: src/quant/ingest/
    est_complexity: small

  - id: C1-M2
    rank: 16
    title: "C1-M2: Point-in-time today's-bar reader + build_features integration"
    project: C
    sub_project: C1
    status: done
    started_at: 2026-06-27T23:55:00Z
    completed_at: 2026-06-28
    depends_on: [C1-M1]
    blocks: [C1-M3, C2-PRD, E2-M1]
    deliverable:
      file: src/quant/storage/realtime.py
      tests: tests/test_realtime.py
      notebook: notebooks/13_c1_live_data.ipynb
    references:
      primary: .claude/prds/c1-live-data.prd.md
    est_complexity: medium

  - id: C1-M3
    rank: 17
    title: "C1-M3: Freshness monitor + alert (cron)"
    project: C
    sub_project: C1
    status: done
    started_at: 2026-06-28T01:58:07Z
    completed_at: 2026-06-28
    depends_on: [C1-M2]
    blocks: [E4-M1]
    deliverable:
      file: scripts/monitor_freshness.py
      cron_doc: docs/concepts/freshness-monitor.md
    references:
      primary: .claude/prds/c1-live-data.prd.md
    est_complexity: small

  # ------ C2 — LEAN execution layer (PRD + milestones) ------

  - id: C2-PRD
    rank: 18
    title: "Draft C2 (LEAN local + paper trading) PRD"
    project: C
    sub_project: C2
    status: done
    started_at: 2026-06-28T02:36:55Z
    completed_at: 2026-06-28
    depends_on: [C1-PRD, C1-M2]
    blocks: [C2-M1, C2-M2, C2-M3]
    deliverable:
      file: .claude/prds/c2-lean-paper.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
      lean_spec_context: docs/historical/PHASE_4_ADVANCED.md
    est_complexity: small
    notes: |
      Platform decision: LEAN local first, Alpaca paper as fallback if LEAN
      install friction exceeds 2 days. Placeholder strategy: ARIMA(1,0,0)
      daily — exercises prediction emission, signal feed, paper execution,
      reconciliation. GBM intentionally not used as placeholder (adds compute
      without exercising additional deployment infrastructure).

  - id: C2-M1
    rank: 19
    title: "C2-M1: LEAN local installed; hello-world algorithm runs"
    project: C
    sub_project: C2
    status: done
    started_at: 2026-06-28T05:00:25Z
    completed_at: 2026-06-28
    depends_on: [C2-PRD]
    blocks: [C2-M2]
    deliverable:
      docs: docs/concepts/lean-setup.md
      script: scripts/c2_hello_world.py
      tests: tests/test_c2_hello_world.py
    references:
      primary: .claude/prds/c2-lean-paper.prd.md
    est_complexity: medium
    notes: |
      PLATFORM DECISION (recorded in docs/concepts/lean-setup.md): LEAN-local was
      attempted (Docker native arm64 ✓, LEAN CLI 1.0.227 ✓) but blocked at
      `lean init` — local data/live use is paywalled behind a paid QuantConnect
      "Quant Researcher" seat. Per the ratified §8.3 fallback (LEAN-first; Alpaca
      paper if friction > 2 days), C2 runs on **Alpaca paper trading** (pure-Python,
      zero Docker; the .env keys already resolve to an Alpaca paper account, PA…).
      LEAN preserved as a documented future swap behind the C2-M2 ExecutionBridge
      Protocol. Hello-world placed one live paper order (BUY 1 SPY → ACCEPTED, then
      cancelled); evidence in lean-setup.md §2. 14 unit tests, 99% module coverage.
      Follow-up C2-DOC-PLATFORM-SYNC appended to sync the PRD/ROADMAP/M2-M3 wording.

  - id: C2-M2
    rank: 20
    title: "C2-M2: ARIMA(1,0,0) daily signal feeds LEAN paper account"
    project: C
    sub_project: C2
    status: done
    started_at: 2026-06-28T07:30:00Z
    completed_at: 2026-06-28
    depends_on: [C2-M1]
    blocks: [C2-M3, C3-PRD, C4-PRD, E3-M1]
    deliverable:
      file: src/quant/execution/lean_bridge.py
      tests: tests/test_lean_bridge.py
    references:
      primary: .claude/prds/c2-lean-paper.prd.md
      platform_decision: docs/concepts/lean-setup.md
    est_complexity: large
    notes: |
      PLATFORM IS ALPACA PAPER, not LEAN — decided in C2-M1 (docs/concepts/
      lean-setup.md): LEAN-local is paywalled, so the ratified §8.3 fallback was
      taken. Build the `ExecutionBridge` Protocol with `AlpacaPaperBridge` as the
      PRIMARY impl (the PRD frames it as the fallback; the decision promotes it to
      primary), `LeanBridge` deferred as the future swap. Reuse the C2-M1 helpers
      in scripts/c2_hello_world.py (build_paper_client / build_hello_order pattern).
      Open module-name question (flagged in C2-DOC-PLATFORM-SYNC): the deliverable
      path is `lean_bridge.py` but the module is now Alpaca-first — keep the
      LEAN-agnostic ExecutionBridge name or rename to e.g. execution/bridge.py;
      resolve before committing. The title still says "LEAN paper account" pending
      the C2-DOC-PLATFORM-SYNC wording sweep.

  - id: C2-M3
    rank: 21
    title: "C2-M3: Backtest-vs-paper reconciliation harness (>1% delta = investigate)"
    project: C
    sub_project: C2
    status: done
    started_at: 2026-06-28T16:30:00Z
    completed_at: 2026-06-28
    depends_on: [C2-M2]
    blocks: []
    deliverable:
      file: scripts/reconcile_paper_backtest.py
      tests: tests/test_reconciliation.py
    references:
      primary: .claude/prds/c2-lean-paper.prd.md
    est_complexity: medium

  # ------ C6 — Strategy registry & daily executor (the deployment spine) ------
  # The missing layer that composes C2 (bridge) + C3 (sizing) + C4 (confidence):
  # a registry of deployable strategies + a daily cron executor over the ENABLED
  # subset. Ranked AHEAD of C3/C4 (contract-before-consumer, METHODOLOGY §4) — the
  # registry schema (C6-M1) is what C3/C4 populate, so it lands first. Captured
  # 2026-06-28 from the multi-model / confidence-sizing design discussion.

  - id: C6-PRD
    rank: 22
    title: "Draft C6 (strategy registry + daily executor) PRD"
    project: C
    sub_project: C6
    status: done
    started_at: 2026-06-28T18:00:00Z
    completed_at: 2026-06-28
    depends_on: [C2-M2]
    blocks: [C6-M1]
    deliverable:
      file: .claude/prds/c6-strategy-registry.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
      execution_bridge: src/quant/execution/lean_bridge.py
      daily_runner: scripts/reconcile_paper_backtest.py
      feature_catalog: src/quant/features/catalog.yaml
      target_catalog: src/quant/features/targets.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Use /plan-prd. Pre-commits (METHODOLOGY §1) the design settled 2026-06-28:
        - STRATEGY = full pipeline spec, not just a model. Schema fields: id,
          display_name, description (human-readable, for the UI), model_ref
          (→ src/quant/models/), feature_set_ref (→ features/catalog.yaml subset),
          target_ref (→ TARGET_CATALOG in features/targets.py), universe,
          decision_rule (forecast→position; the C2 sign() parity rule),
          sizing_policy (C3 fills), confidence_gate (C4 fills), risk_limits (C3:
          max position, drawdown stop), cadence (daily, fixed), broker
          (ExecutionBridge selector; paper now), enabled, provenance,
          created/enabled timestamps. REFERENCES the existing catalogs — does NOT
          duplicate them (DRY / §4). It is the DEPLOYMENT-side subset of Project
          B's research (target,model,universe) portfolio, promoted-to-live.
        - PROVENANCE GATE (methodology guardrail): a strategy may not be
          enabled:true without provenance pointing at a passing gate verdict
          (ledger entry / DSR-aware gate). arima_placeholder is the one sanctioned
          exception (provenance: placeholder, n_comparisons=0 — infra, not an edge
          claim). Carries "no edge without a pre-committed gate" into deployment.
        - DAILY ALLOCATOR (pinned): each cron run, for every ENABLED strategy:
          predict → size (C3×C4) → signed target positions; combine across
          strategies by (a) equal-weight CAPITAL BUDGET per enabled strategy (1/N;
          one config field, later swappable for confidence/track-record weighting),
          (b) confidence enters ONCE at sizing — a high-confidence strategy already
          deploys a larger position, no separate weighting, (c) NET the signed
          sized positions per symbol and clamp to the per-symbol risk cap. Opposed
          signals net to a residual.
        - INITIAL STATE: exactly one enabled entry (arima_placeholder) → 100% of
          the (1/N=1) budget → FULLY-INVESTED EQUAL-WEIGHT-WITHIN-UNIVERSE sizing
          (until C3), so the portfolio/sizing/netting path is exercised end-to-end
          and the C2-M2-SIZING-PARITY gap is closed here. P&L stays uninteresting
          (ARIMA on a few names) by design.
        - UI CONTRACT (Project E): the registry exposes UI-presentable fields + a
          documented serializable view-model (in-use AND idle strategies) the
          console export consumes. The panel itself is E-STRATEGIES-PANEL
          (depends_on C6-M1) — coordinate with the Project E owner.
        - PAPER ONLY: broker=alpaca_paper; ExecutionBridge already abstracts paper
          vs live, so live is a later config flag, out of scope here.

  - id: C6-M1
    rank: 23
    title: "C6-M1: Strategy registry schema + loader + drift test"
    project: C
    sub_project: C6
    status: done
    started_at: 2026-06-28T16:57:08Z
    completed_at: 2026-06-28
    depends_on: [C6-PRD]
    blocks: [C6-M2, C3-PRD, C4-PRD, E-STRATEGIES-PANEL]
    deliverable:
      file: src/quant/execution/strategy_registry.py
      registry: src/quant/execution/strategy_registry.yaml
      tests: tests/test_strategy_registry.py
    references:
      primary: .claude/prds/c6-strategy-registry.prd.md
      pattern: src/quant/features/catalog.py
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      The contract C3/C4 + the daily executor + the E console all consume. Mirror
      the features/catalog.{py,yaml} pattern: a YAML registry + a typed loader + a
      code-vs-config drift test in BOTH directions (METHODOLOGY §6). Validate that
      refs resolve (model_ref → models/, feature_set_ref → catalog, target_ref →
      TARGET_CATALOG) and enforce the provenance gate (enabled ⇒ provenance
      present; placeholder exempt). Expose a serializable view-model for the E
      console. Seed one entry: arima_placeholder (enabled, provenance: placeholder).

  - id: C6-M2
    rank: 24
    title: "C6-M2: Daily cron executor (trade_daily.py) over the enabled registry"
    project: C
    sub_project: C6
    status: done
    started_at: 2026-06-28T19:05:00Z
    completed_at: 2026-06-28
    depends_on: [C6-M1, C1-M3, C2-M2]
    blocks: []
    deliverable:
      file: scripts/trade_daily.py
      tests: tests/test_trade_daily.py
    references:
      primary: .claude/prds/c6-strategy-registry.prd.md
      bridge: src/quant/execution/lean_bridge.py
      freshness: scripts/monitor_freshness.py
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      THE "runs continually" deliverable. One idempotent entrypoint for the daily
      cron: ingest (quant.flows.daily) → freshness gate (monitor_freshness) → for
      each ENABLED strategy: daily_signal via its model → fully-invested
      equal-weight sizing (placeholder until C3) → NET per symbol → place_target
      via AlpacaPaperBridge → persist position state. Exits non-zero on any failure
      (cron mail-on-stderr); re-runnable same-day (position-state round-trip). Cron
      doc: run after the parity-safe Tiingo T+1 bar (~12:00 UTC), weekdays. Paper
      only.

  # ------ C3 / C4 / C5 — PRD drafting and milestones (sketch) ------

  - id: C3-PRD
    rank: 71
    title: "Draft C3 (position sizing + risk management) PRD"
    project: C
    sub_project: C3
    status: ready
    depends_on: [C2-M2, C6-M1]
    blocks: [E3-M1]
    deliverable:
      file: .claude/prds/c3-sizing-risk.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
    est_complexity: small
    notes: |
      Re-pointed at C6-M1 (the strategy-registry contract) on 2026-06-28: C3's
      sizing_policy + risk_limits are FIELDS the registry holds, so C3 is drafted
      against the contract rather than standalone (contract-before-consumer, §4).
      Re-ranked to 71 behind the C6 spine; still a high-priority C-execution PRD —
      re-prioritize (bump rank back into the C band) once C6-M1 lands.

  - id: C4-PRD
    rank: 72
    title: "Draft C4 (confidence calibration) PRD"
    project: C
    sub_project: C4
    status: ready
    depends_on: [C2-M2, C6-M1]
    blocks: []
    deliverable:
      file: .claude/prds/c4-confidence.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
    est_complexity: small
    notes: |
      Re-pointed at C6-M1 (the strategy-registry contract) on 2026-06-28: C4's
      confidence_gate is a FIELD the registry holds (contract-before-consumer, §4).
      Re-ranked to 72 behind the C6 spine; re-prioritize once C6-M1 lands.

      Boundary with Project E (reconciled during Project E task generation):
      live CALIBRATION-drift monitoring (a 90% interval no longer covering 90%
      on live data) is C4's scope — the live extension of C4-M3's per-regime
      calibration audit — and is SURFACED in the console (E3/E4) but COMPUTED
      by C4's calibration machinery. E4 owns FEATURE/DATA-distribution drift,
      a different signal. Do not duplicate calibration-drift logic in E4. See
      PROJECT_ROADMAP §7 (C4 sketch boundary note + the C5-superseded note).

  - id: C5-PRD
    rank: 73
    title: "Draft C5 (monitoring + reconciliation dashboard) PRD — SKIPPED (superseded by Project E3/E4)"
    project: C
    sub_project: C5
    status: skipped
    depends_on: [C2-M3]
    blocks: []
    deliverable:
      file: .claude/prds/c5-monitoring.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
      superseded_by: docs/project-e/E3-live-monitoring.prd.md
    est_complexity: small
    notes: |
      SKIPPED — Project E3 (Live Monitoring) + E4 (Data & Market Status)
      supersede C5; no standalone C5 PRD is needed. C5's scope was relocated
      during Project E task generation (see PROJECT_ROADMAP §7 C5-superseded
      note for the full redistribution):
        - dashboard (positions / P&L / exposure / paper-vs-backtest delta /
          live regime indicator / model-output histogram) -> E3
        - paper-backtest divergence alerting -> E3 (E3-M3 reconciliation)
        - regime-change alert + feed staleness + gap + feature-drift -> E4
        - calibration-drift monitoring -> C4 (live extension of C4-M3)
      The two C5 elements not already in the E3/E4 PRDs — the model-output
      (signal-distribution) histogram and regime-change alerting — were folded
      into the E3 and E4 PRDs respectively as part of this skip.
      Removed from C-CLOSE.depends_on (skipped != done would deadlock the
      closeout's "all deps done" gate). If C5 is ever revived, redraft this PRD
      and re-add it to C-CLOSE.depends_on.

  # ------ Conditional follow-on B sub-projects (queued) ------

  - id: B3-PRD
    rank: 25
    title: "Draft B3 (alternative data — options-implied surfaces) PRD"
    project: B
    sub_project: B3
    status: done
    started_at: 2026-06-28T18:22:23Z
    completed_at: 2026-06-28
    depends_on: [B1-M3]
    blocks: [B3-M1, B3-M2, B3-M3]
    deliverable:
      file: .claude/prds/b3-options-implied.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
    est_complexity: small
    notes: |
      Conditional: only draft if B1 verdict is "no extractable edge on any
      of the four targets with the current feature set."
      ACTIVATED 2026-06-28: B1_REPORT.md §4 records a clean pre-committed NO-GO
      on all four targets → the B3 skip-path fires (METHODOLOGY §5). PRD drafted
      mirroring b1-target-reframing.prd.md. Scope pinned to FREE market-level Cboe
      data (VIX term structure, SKEW, put-call ratios) as PIT-lagged panel columns;
      paid per-symbol IV surfaces are a separate cost-gated PRD. Gate is an
      INCREMENTAL feature-group ablation (augmented vs M6 equity-only) reusing the
      B1 TARGET_CATALOG + b1_gate_report. Milestones B3-M1/M2/M3 appended below.

  # ------ B3 execution milestones (appended 2026-06-28 when B3-PRD landed) ------
  # Parked at ranks 80-82 (the "queued, re-rank when prioritized" convention used
  # by C3-PRD/C4-PRD): B3 is the roadmap's "conditional, queued" alt-data thread,
  # ranked behind the active C-execution spine. depends_on is the hard ordering;
  # bump these into the active band when B3 execution is scheduled.

  - id: B3-M1
    rank: 80
    title: "B3-M1: Cboe ingestor + options-implied feature group + catalog + b3_gate_report"
    project: B
    sub_project: B3
    status: ready
    depends_on: [B3-PRD]
    blocks: [B3-M2, B-CLOSE]
    deliverable:
      file: src/quant/ingest/cboe.py
      feature_columns: src/quant/features/engineering.py
      catalog: src/quant/features/catalog.yaml
      gate: src/quant/backtest/regime_metrics.py
      tests: tests/test_cboe.py
    references:
      primary: .claude/prds/b3-options-implied.prd.md
      methodology: docs/METHODOLOGY.md
      ingestor_pattern: src/quant/ingest/fred_macro.py
      gate_pattern: src/quant/backtest/regime_metrics.py
    est_complexity: medium
    notes: |
      Contract-before-consumer (METHODOLOGY §2/§4). Free Cboe daily settle only:
      VIX/VIX3M/VIX9D term structure, SKEW, equity/index/total put-call ratios,
      pulled to the lake via storage/lake.write_processed with a pandera schema
      (mirror ingest/fred_macro.py). Wire as PIT-LAGGED panel-constant columns in
      build_features (the FRED_PUBLICATION_LAGS join pattern) + a frozen a-priori
      transform set (vix_term_slope, vix_short_ratio, SKEW level, P/C z-score).
      Catalog records under the bidirectional drift test (tests/test_catalog.py).
      b3_gate_report wraps b1_gate_report with the comparison fixed to augmented-vs-
      equity-only and the incremental thresholds pinned in the PRD; reads N via
      quant.ledger.cumulative_trial_count(). MUST assert the options publication lag
      (pinned lag 0; fall back to t+1) against backtest/CLAUDE.md invariants 3-4 and
      report per-feature × per-regime coverage (VIX9D ~2011 / VIX3M ~2007 starts).

  - id: B3-M2
    rank: 81
    title: "B3-M2: Options feature-group ablation matrix on 5-symbol × 8-year slice"
    project: B
    sub_project: B3
    status: blocked
    depends_on: [B3-M1]
    blocks: [B3-M3, B-CLOSE]
    deliverable:
      notebook: notebooks/14_b3_options_ablation.ipynb
    references:
      primary: .claude/prds/b3-options-implied.prd.md
      methodology: docs/METHODOLOGY.md
      ablation_pattern: src/quant/backtest/ablation.py
    est_complexity: medium
    notes: |
      Add-one feature-group ablation per the four B1 targets: {M6 equity-only base,
      M6 + options group augmented} via backtest/ablation.make_add_one_sets. Verdict
      via b3_gate_report on the INCREMENTAL delta; Borda composite across regimes.
      Slice verdict is PROVISIONAL (METHODOLOGY §11); any target with incremental
      per-regime edge carries to B3-M3.

  - id: B3-M3
    rank: 82
    title: "B3-M3: Full-panel confirmation of slice-winners (compute-gated) + B3_REPORT"
    project: B
    sub_project: B3
    status: blocked
    depends_on: [B3-M2]
    blocks: [B4-PRD, B-CLOSE]
    deliverable:
      runner: scripts/run_b3_arms.py
      notebook: notebooks/16_b3_exit_gate.ipynb
      report: docs/B3_REPORT.md
    references:
      primary: .claude/prds/b3-options-implied.prd.md
      methodology: docs/METHODOLOGY.md
      runner_pattern: scripts/run_b1_arms.py
    est_complexity: large
    notes: |
      Mirror run_b1_arms.py: per-arm parquet checkpoints, idempotent, --smoke,
      --log-ledger (one ledger entry per arm via quant.ledger.append_ledger_entry so
      the deflation N stays current). notebooks/15 is checkpoint-only (METHODOLOGY
      §7). docs/B3_REPORT.md is the binary verdict (the §20/§21 analog of
      B1_REPORT.md). The B3 verdict is the gate that B4-PRD's conditional consumes
      (B4 drafts only if B1 AND B3 both surface no edge).

  - id: B4-PRD
    rank: 26
    title: "Draft B4 (universe shift — mid-cap / single-stock options / international) PRD"
    project: B
    sub_project: B4
    status: blocked
    depends_on: [B1-M3, B3-M3]
    blocks: []
    deliverable:
      file: .claude/prds/b4-universe-shift.prd.md
    references:
      primary: docs/PROJECT_ROADMAP.md
    est_complexity: small
    notes: |
      Conditional: only draft if B1 + B3 do not surface edge. Lowest priority
      in Project B. depends_on repointed B3-PRD -> B3-M3 on 2026-06-28 (B3-PRD
      landing): B4's conditional is gated on B3's EXECUTION verdict (B3-M3), not the
      B3 draft — mirrors B3-PRD itself depending on B1-M3 (the verdict), not B1-PRD.

  # ------ Methodology doc-sync follow-ups (discovered during PRD drafting) ------

  - id: A-METH-NONSHARPE-SYNC
    rank: 27
    title: "Sync METHODOLOGY Open-Questions: non-Sharpe materiality thresholds now set in B1 PRD"
    project: A
    sub_project: methodology-upgrade
    status: done
    started_at: 2026-06-28T19:40:00Z
    completed_at: 2026-06-28
    depends_on: [B1-PRD]
    blocks: []
    deliverable:
      file: docs/METHODOLOGY.md
    references:
      primary: .claude/prds/b1-target-reframing.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B1-PRD drafting (commit of
      .claude/prds/b1-target-reframing.prd.md). METHODOLOGY.md §"Open questions
      for the next research PRD" lists "Materiality thresholds for non-Sharpe
      targets ... Set during Project B1 / C4 PRD drafting" as still-open. The
      B1 PRD now PINS the classification (ΔAUC ≥ 0.02) and vol-regression
      (ΔMAE ≥ 5% relative) materiality thresholds. Update that open-questions
      bullet to mark the B1 portion resolved and link the B1 PRD's "Success
      Metrics" table; leave the C4 (confidence-calibration) portion open. Do
      NOT touch any numbered rule (§1–§21) — this is a forward-looking
      notes-section sync only (AGENT_OPERATION red-flag: no rule edits without
      a roadmap decision). Non-gating; low priority.

  - id: A-METH-OOSATTR-SYNC
    rank: 28
    title: "Sync METHODOLOGY Open-Questions: OOS attribution method now picked in B2 PRD"
    project: A
    sub_project: methodology-upgrade
    status: done
    started_at: 2026-06-28T20:30:00Z
    completed_at: 2026-06-28
    depends_on: [B2-PRD]
    blocks: []
    deliverable:
      file: docs/METHODOLOGY.md
    references:
      primary: .claude/prds/b2-oos-attribution.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B2-PRD drafting (commit of
      .claude/prds/b2-oos-attribution.prd.md). METHODOLOGY.md §"Open questions
      for the next research PRD" lists "OOS attribution method. Permutation
      importance computed OOS, per-fold ablation as canonical, or conformal
      feature relevance? Pick in Project B2." as still-open. The B2 PRD now makes
      the pick: implement per-fold ablation as the canonical signal + OOS
      permutation importance (validated against it at Spearman ρ ≥ 0.50), with
      conformal feature relevance deferred as a conditional follow-on. Update that
      open-questions bullet to mark it resolved-in-PRD and link the B2 PRD's
      "Success Metrics"/"Pre-committed gate" sections; note the method ships in
      code at B2-M2 and lands in the catalog at B2-M3 (Phase-5 Trigger 2). Do NOT
      touch any numbered rule (§1–§21) — this is a forward-looking notes-section
      sync only (AGENT_OPERATION red-flag: no rule edits without a roadmap
      decision). Mirrors A-METH-NONSHARPE-SYNC. Non-gating; low priority.

  - id: A-PRIORITIES-TEST-TS
    rank: 31
    title: "Extend tests/test_priorities.py: timestamp presence + ordering (started_at on in_progress/done; completed_at >= started_at)"
    project: A
    sub_project: methodology-upgrade
    status: done
    started_at: 2026-06-28T21:30:00Z
    completed_at: 2026-06-28
    depends_on: [A-PRIORITIES-TEST]
    blocks: [A-CLOSE]
    deliverable:
      file: tests/test_priorities.py
    references:
      primary: docs/PRIORITIES.yaml
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during A-PRIORITIES-TEST post-task review (commit of
      tests/test_priorities.py). The four invariants that task pinned
      (status enum, reference resolution, single in_progress, done-has-
      completed_at) do NOT cover the timestamp fields, yet AGENT_OPERATION
      Step 3 requires `started_at` on any claimed task and Step 8 requires
      `completed_at` on `done`. Add checks: (a) every `in_progress` or `done`
      task has `started_at`; (b) `completed_at >= started_at` (date ordering)
      where both present; (c) timestamps parse as ISO dates/datetimes. Mirror
      the existing positive/negative validator pattern in the file. Non-gating;
      low priority. Added to A-CLOSE.depends_on per AGENT_OPERATION Step 7
      corollary (new Project-A task appended after A-CLOSE exists).

  - id: A-DSR-LEDGER-SHARPE
    rank: 32
    title: "Store per-trial Sharpe in the ledger; compute empirical sharpe_std for the DSR benchmark"
    project: A
    sub_project: methodology-upgrade-13
    status: done
    started_at: 2026-06-28T22:30:00Z
    completed_at: 2026-06-28
    depends_on: [A-LEDGER, A-DSR-GATE]
    blocks: [A-CLOSE]
    deliverable:
      file: src/quant/ledger.py
      tests: tests/test_ledger.py
    references:
      primary: src/quant/backtest/statistics.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during A-DSR-GATE post-task review (commit of
      src/quant/backtest/statistics.py deflated_sharpe_ratio). The canonical
      Bailey-López de Prado (2014) DSR deflates against the empirical
      cross-trial Sharpe variance V̂[{SR_n}]; our implementation uses a PINNED
      scalar (statistics.DEFAULT_SHARPE_STD = 0.35, the rough M3 noise-guard
      estimate from PHASE_4A_REPORT §7) because data/ledger.yaml stores
      n_comparisons (counts), not per-trial Sharpes. Add an optional `sharpe`
      field to LedgerEntry (back-compat: default None / omitted) and a helper
      (e.g. quant.ledger.observed_sharpe_std()) that computes the std of the
      recorded trial Sharpes, so dsr_aware_gate_report can pass an EMPIRICAL
      sharpe_std instead of the scalar default. Non-gating; the scalar default
      keeps the gate correct meanwhile. Low priority. Added to A-CLOSE.depends_on
      per AGENT_OPERATION Step 7 corollary.

  - id: A-DSR-DOC-SYNC
    rank: 33
    title: "Sync evaluation-standards.md T4 to reference the new deflated_sharpe_ratio implementation"
    project: A
    sub_project: methodology-upgrade
    status: done
    started_at: 2026-06-28T23:30:00Z
    completed_at: 2026-06-28
    depends_on: [A-DSR-GATE]
    blocks: [A-CLOSE]
    deliverable:
      file: docs/concepts/evaluation-standards.md
    references:
      primary: src/quant/backtest/statistics.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during A-DSR-GATE post-task review (commit of
      src/quant/backtest/statistics.py). evaluation-standards.md T4 describes the
      DSR implementation in the present tense ("The implementation computes DSR
      as `norm.cdf((sr - E[max_sr]) / se_sr)`") but no implementation existed
      until A-DSR-GATE. Update T4 to point at
      quant.backtest.statistics.deflated_sharpe_ratio + expected_max_sharpe and
      quant.backtest.regime_metrics.dsr_aware_gate_report as the canonical code,
      and note the DSR>0.5 ⟺ "deflated excess Sharpe > 0" ⟺ "SR > E[max]"
      equivalence the implementation pins (METHODOLOGY §13). Also flag that the
      T4 N (hyperparameter configs, ≤50) and the §12 ledger N (cumulative trials)
      are DIFFERENT deflation populations — the gate uses the ledger N. Doc-only;
      do NOT change the T4 = 0.5 threshold. Mirrors A-METH-*-SYNC. Non-gating;
      low priority. Added to A-CLOSE.depends_on per Step 7 corollary.

  - id: B1-TARGETS-CONCEPT-DOC
    rank: 34
    title: "Concept doc for the B1 candidate targets (docs/concepts/target-reframing.md)"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-28T22:26:49Z
    completed_at: 2026-06-28
    depends_on: [B1-M1]
    blocks: [B-CLOSE]
    deliverable:
      file: docs/concepts/target-reframing.md
    references:
      primary: src/quant/features/targets.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B1-M1 post-task review (commit of
      src/quant/features/targets.py). The four B1 targets, their point-in-time
      rules, and the pinned materiality thresholds live only in targets.py
      docstrings + the B1 PRD; every other label family in the repo has a
      companion concept doc (features/label_schemes.py <-> docs/concepts/
      label-schemes.md). Write docs/concepts/target-reframing.md mirroring
      label-schemes.md: per-target definition + PIT rule + horizon, the
      flat-module-vs-catalog decision, the deflation-method choice per target
      (dsr vs skill_z, including the T1 "DSR-literal vs skill-z analog" note),
      and the 5/21-bar horizon embargo-coupling caveat. Doc-only; do NOT change
      any pinned threshold (those stay sourced from targets.py / b1_gate_report).
      Non-gating; low priority. Added to B-CLOSE.depends_on per AGENT_OPERATION
      Step 7 corollary (new Project-B task appended after B-CLOSE exists).

  - id: B1-TARGETEVAL-DOC
    rank: 35
    title: "Concept doc for backtest/target_eval.py (OOS-prediction collector + skill-z + directional-signal convention)"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-28T21:30:00Z
    completed_at: 2026-06-28
    depends_on: [B1-M2]
    blocks: [B-CLOSE]
    deliverable:
      file: docs/concepts/target-evaluation.md
    references:
      primary: src/quant/backtest/target_eval.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B1-M2 (commit of src/quant/backtest/target_eval.py +
      notebooks/10_b1_target_ablation.ipynb). B1-M2 added a new backtest module
      (target_eval: collect_oos_predictions / simulate_signal_returns /
      per_regime_metric) and a new statistics helper (forecast_skill_z), but unlike
      every other backtest sub-module (regimes <-> regime-evaluation.md, label_schemes
      <-> label-schemes.md) it has no companion concept doc. Write
      docs/concepts/target-evaluation.md covering: why the return-simulator harness
      cannot score AUC/MAE (the motivation), the purged-walk-forward prediction
      collector contract, the directional Sharpe-arm sign(pred-0.5) convention (vs the
      harness's sign(pred) at 0), and the skill-z deflation analog (Brier/abs-err
      improvement z-score) for non-tradeable targets. Doc-only; do NOT change pinned
      thresholds. Non-gating; low priority. Added to B-CLOSE.depends_on per
      AGENT_OPERATION Step 7 corollary.

  - id: B1-DD-VOLIMPLIED-BASELINE
    rank: 36
    title: "Formalize the T1 drawdown 'vol-implied DD probability' baseline (currently an EWMA-vol-rank proxy)"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-28T22:10:00Z
    completed_at: 2026-06-28
    depends_on: [B1-M2]
    blocks: [B-CLOSE]
    deliverable:
      file: notebooks/10_b1_target_ablation.ipynb
    references:
      primary: .claude/prds/b1-target-reframing.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B1-M2 (declared deviation, METHODOLOGY §9). The B1 PRD's T1
      baseline is "ARIMA-vol-implied DD probability"; nb10 implements it as an
      EWMA(0.94)-daily-vol score ranked against the 0/1 drawdown label (higher vol ->
      higher DD prob) — a defensible monotone proxy but NOT a calibrated probability or
      an ARIMA-on-vol forecast. On the slice this proxy was already STRONG (AUC 0.712
      covid / 0.653 rate_cycle, beating the GBM's 0.622 / 0.508), so refining it only
      strengthens the clean negative — hence low priority. If B1-M3 revisits T1, swap to
      a principled vol-implied DD probability (e.g. map an ARIMA-on-log-vol forecast
      through a parametric/empirical DD-probability function) under a new ledger entry.
      Non-gating; low priority. Added to B-CLOSE.depends_on per AGENT_OPERATION Step 7
      corollary.

  - id: B1-M3-TZTEST
    rank: 37
    title: "Regression test for run_b1_arms tz-alignment (NY price index vs UTC feature index)"
    project: B
    sub_project: B1
    status: done
    started_at: 2026-06-28T23:30:00Z
    completed_at: 2026-06-28
    depends_on: []
    blocks: [B-CLOSE]
    deliverable:
      file: tests/test_run_b1_arms.py
    references:
      primary: scripts/run_b1_arms.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B1-M3 (commit of scripts/run_b1_arms.py + docs/B1_REPORT.md
      §5 declared deviation). The first full-panel launch dropped all 33 symbols:
      the lake price index is America/New_York (close 20:00-04:00) while
      build_features returns UTC, and a tz_localize(None) strip mis-aligned feature
      vs label indices by the UTC offset (feature 00:00 UTC vs label 20:00 naive).
      Fixed with run_b1_arms._to_naive_utc (tz_convert->strip, preserving the
      instant) and verified manually on real 6-symbol data, but the smoke test could
      NOT catch it — the synthetic panel is tz-naive, so _to_naive_utc is a no-op
      there. Add a regression test that feeds a tz-AWARE (NY) price frame + a UTC-tz
      feature frame through _to_naive_utc / _aligned_target_panel and asserts
      feature.index.equals(label.index) and a non-empty aligned panel, so the
      alignment contract is covered, not merely exercised. Non-gating; the fix is
      live in the committed runner. Added to B-CLOSE.depends_on per AGENT_OPERATION
      Step 7 corollary.

  - id: B2-DOC-COLCOUNT
    rank: 38
    title: "Reconcile the attributed feature set: 25-col M6 model set vs 27-col catalog"
    project: B
    sub_project: B2
    status: in_progress
    started_at: 2026-06-28T21:30:00Z
    depends_on: [B2-M1]
    blocks: [B-CLOSE]
    deliverable:
      file: docs/concepts/oos-attribution.md
    references:
      primary: src/quant/features/catalog.yaml
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B2-M1 post-task review (commit of
      docs/concepts/oos-attribution.md). The doc uses the B2 PRD's "25-column M6
      feature set" as the attribution validation surface, while CLAUDE.md and
      features/catalog.yaml reference 27 registered columns. These are two
      different things (the M6 model INPUT set is a 25-col subset; the catalog
      registers 27 known features), not an error — but B2-M2/M3 must pin exactly
      which set gets an OOS attribution score and which catalog rows receive the
      attribution_status field. Resolve the count, align the oos-attribution.md
      wording and the catalog references, and state the attributed set explicitly
      before B2-M3 populates the field. Doc/clarification only; non-gating. Added
      to B-CLOSE.depends_on per AGENT_OPERATION Step 7 corollary.

  - id: B2-M2-G2-NB08
    rank: 39
    title: "G2 exact reproduction: systematized ablation vs nb08's frozen best-regime lifts"
    project: B
    sub_project: B2
    status: ready
    depends_on: [B2-M2]
    blocks: [B-CLOSE]
    deliverable:
      file: scripts/run_b2_attribution.py
      tests: tests/test_run_b2_attribution.py
    references:
      primary: .claude/prds/b2-oos-attribution.prd.md
      methodology: docs/METHODOLOGY.md
      nb08: notebooks/08_phase4a_feature_ablation.ipynb
    est_complexity: small
    notes: |
      Discovered during B2-M2 (commit of scripts/run_b2_attribution.py — DECLARED
      DEVIATION, METHODOLOGY §9). The B2 PRD's G2 ("systematized ablation reproduces
      M3, Spearman ρ ≥ 0.90 vs nb08's published per-feature lifts") names nb08's
      *best-regime add-one lift* as the reference. The runner instead computes an
      *aggregate-OOS-Sharpe* add-one lift (Sharpe(base17+c) − Sharpe(base17)) as the
      G2 reference, because nb08's per-candidate lift vector is NOT frozen to a file
      (it is recomputed at notebook runtime; cf. B2-DOC-COLCOUNT's 25-vs-27 surface
      question). Two gaps to close: (a) freeze nb08's published best-regime lifts as a
      checkpointed reference vector (or recompute via compute_regime_metrics on the
      add-one results), and (b) point b2_attribution_gate's `reproduction` arg at it,
      so G2 reproduces nb08's *exact* statistic rather than the aggregate proxy. Until
      then the real-slice G2 ρ is provisional. Non-gating for the B2-M2 module (the
      gate function + tests are correct); blocks B-CLOSE so the assembled-B verdict is
      not certified on the proxy. Added to B-CLOSE.depends_on per Step 7 corollary.

  - id: B2-FULLPANEL-AGREEMENT
    rank: 40
    title: "Optional full-panel G1 agreement confirmation (permutation vs ablation, 33-symbol panel)"
    project: B
    sub_project: B2
    status: ready
    depends_on: [B2-M2]
    blocks: []
    deliverable:
      runner: scripts/run_b2_attribution.py
    references:
      primary: .claude/prds/b2-oos-attribution.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B2-M2. The B2 PRD pins G1/G2 to the 5-symbol × 8-year slice
      (the like-for-like nb08 reproduction surface) and explicitly flags a full-panel
      agreement check as a "desirable confirmation but NOT an MVP gate" (B2 validates a
      method, not an edge). Add a --panel mode to run_b2_attribution that runs G1
      (permutation vs ablation) on the full 33-symbol union panel and reports whether
      the slice agreement survives at scale; record it in the catalog
      attribution_status audit trail. CONDITIONAL / non-MVP, mirroring B3-PRD/B4-PRD —
      intentionally NOT in B-CLOSE.depends_on so an unrun optional confirmation cannot
      deadlock closeout. Low priority.

  - id: B2-CONFORMAL-PRD
    rank: 41
    title: "Draft B2 conformal-feature-relevance follow-on PRD (triggered by B2 G1 fail)"
    project: B
    sub_project: B2
    status: ready
    depends_on: [B2-M2]
    blocks: []
    deliverable:
      file: .claude/prds/b2-conformal-relevance.prd.md
    references:
      primary: docs/concepts/oos-attribution.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during B2-M3 (the catalog-population run consumed the B2-M2
      real-slice checkpoint, data/b2/slice/, ledger entry ledger-2026-06-27-0005).
      The B2 G1 gate FAILED on the real 5x8 slice: Spearman rho(permutation,
      ablation) = -0.004 (p = 0.505), far below the pinned 0.50 bar — OOS
      permutation importance is NOT a validated cheap proxy for per-fold ablation
      on this problem (G3 rho = 0.039 confirms IS-importance still does not
      transfer). Per METHODOLOGY §5 (binding conditional trigger/skip path) and the
      B2 PRD "Sequencing notes" / "Conditional follow-on", a FAILED G1 TRIGGERS
      drafting the deferred third method — conformal feature relevance — as the next
      attempt at a cheap OOS-attribution proxy. Per-fold ablation remains the SOLE
      canonical OOS signal in the interim (METHODOLOGY §14 unchanged). A failed G1
      cannot be revived by retuning rho_threshold (that needs a new PRD + ledger
      entry); this conformal PRD is that new attempt. CONDITIONAL research follow-on
      — like B3-PRD/B4-PRD it is intentionally NOT in B-CLOSE.depends_on (it may
      terminate `skipped` if conformal is judged not worth the implementation risk,
      which would deadlock a closeout that depended on it). Use /plan-prd.

  - id: C1-M1-MEASURE
    rank: 42
    title: "Live-measure per-source data availability times to confirm/tighten the C1 freshness SLAs"
    project: C
    sub_project: C1
    status: ready
    depends_on: [C1-M1]
    blocks: []
    deliverable:
      file: docs/concepts/data-freshness-slas.md
    references:
      primary: docs/concepts/data-freshness-slas.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C1-M1 (declared deviation, METHODOLOGY §9, in
      docs/concepts/data-freshness-slas.md "Declared deviations"). The per-source
      "available by" times in the C1-M1 audit are DESK-RESEARCH / code-derived
      estimates of each publisher's release schedule (Alpaca IEX settle, Tiingo
      adjusted EOD, FRED release calendar, EDGAR/RSS liveness), not the output of a
      multi-day live-measured pull that timestamps the first moment today's bar is
      actually fetchable. They are deliberately conservative so the SLA does not
      false-alarm. This task instruments a live daily run for >= 2 weeks to record
      the real first-availability time per feed and either confirms or LOOSENS the
      pinned SLA values (it can only loosen the conservatism). Any change to a
      pinned SLA value follows the C1-M1 update protocol (PRD revision + new ledger
      entry) and updates this doc AND the C1-M3 module constant in lock-step (the
      drift contract). CONDITIONAL / non-MVP confirmation — the SLA is already
      pinned in the ratified C1 PRD and the C1-M1 audit confirms it; per the
      B2-FULLPANEL-AGREEMENT precedent this is intentionally NOT in
      C-CLOSE.depends_on so an unrun optional measurement cannot deadlock closeout.
      Low priority.

  - id: C1-M2-ALPACA-FRESHNESS
    rank: 43
    title: "Reconcile same-day reader source: Tiingo (T+1) parity vs Alpaca (T-evening) freshness"
    project: C
    sub_project: C1
    status: ready
    depends_on: [C1-M2]
    blocks: []
    deliverable:
      file: docs/concepts/data-freshness-slas.md
    references:
      primary: src/quant/storage/realtime.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C1-M2 (commit of src/quant/storage/realtime.py). The PIT
      reader defaults to PRICE_DATASET = "equity_eod_tiingo" because that is the
      dataset the backtest trains on, which makes the G2 train/serve-parity gate
      STRUCTURAL (max_rel_diff = 0.0 on the real lake, nb13). But the C1-M1 SLA
      table pins Tiingo availability at 12:00 UTC on T+1, while the freshest
      same-day feed is Alpaca IEX at 23:00 UTC on T — so a genuine "trade on
      today's bar the evening of T" run cannot use the parity-safe Tiingo source
      yet (Tiingo's adjusted bar for T is not available until T+1). Switching the
      reader to Alpaca for same-day-on-T would BREAK G2 (IEX raw close != Tiingo
      adjClose), i.e. it is a train/serve-skew source, not a drop-in. Resolve the
      deployment policy for C2: either (a) accept a T+1 decision cadence on the
      parity-safe Tiingo source, or (b) introduce an explicit Alpaca same-day
      path with its OWN parity gate against an Alpaca-trained model. Doc/finding
      only; the committed reader is correct for the parity contract. CONDITIONAL
      deployment finding — like B2-FULLPANEL-AGREEMENT it is intentionally NOT in
      C-CLOSE.depends_on (it is a C2 policy decision, not a C1 deliverable). Low
      priority.

  - id: C1-M2-LEDGER-AUDIT
    rank: 44
    title: "Optional audit-only ledger entry (n_comparisons=0) for the nb13 C1-M2 verdict run"
    project: C
    sub_project: C1
    status: ready
    depends_on: [C1-M2]
    blocks: []
    deliverable:
      notebook: notebooks/13_c1_live_data.ipynb
    references:
      primary: src/quant/ledger.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C1-M2 post-task review (commit of
      notebooks/13_c1_live_data.ipynb). The C1 PRD "Ledger discipline" says a C1
      milestone run that emits a verdict artifact "MAY record an audit-only ledger
      entry (n_comparisons = 0)" per the A-LEDGER-RUNNERS pattern — bookkeeping,
      not a deflation contribution (C1 makes no Sharpe claim). nb13 emits a
      G1/G2 verdict but no ledger entry was wired. Add an optional
      quant.ledger.append_ledger_entry call (n_comparisons=0, verdict from the
      gate functions) so the C1-M2 gate run is recorded in data/ledger.yaml for
      audit symmetry with the Phase 4A / B1 runner integrations. Optional per the
      PRD ("may"); intentionally NOT in C-CLOSE.depends_on. Low priority.

  - id: C1-M3-SLA-DOC-DRIFT
    rank: 45
    title: "Close the code-vs-doc §6 gap: machine-readable SLA table or doc-parse drift test for the freshness monitor"
    project: C
    sub_project: C1
    status: ready
    depends_on: [C1-M3]
    blocks: []
    deliverable:
      file: tests/test_monitor_freshness.py
    references:
      primary: scripts/monitor_freshness.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C1-M3 post-task review (commit of
      scripts/monitor_freshness.py + tests/test_monitor_freshness.py). METHODOLOGY
      §6 wants code-vs-config drift contracts in both directions; the reference
      implementation (tests/test_catalog.py) compares code against a
      MACHINE-READABLE catalog.yaml. The freshness SLA values are pinned as module
      constants (scripts/monitor_freshness.py::SOURCE_SLAS) and asserted against
      literal values in tests/test_monitor_freshness.py
      (test_pinned_sla_constants_match_frozen_contract,
      test_monitored_set_is_exactly_the_c1m1_table), which trips CI on a CODE edit
      — but the C1-M1 contract (docs/concepts/data-freshness-slas.md) is a
      human-readable Markdown TABLE, so nothing asserts code == the DOC. The doc
      and code could silently diverge (only the C1-M1 "Update protocol" social
      contract keeps them in lock-step). Close the gap by either (a) extracting the
      SLA table to a small machine-readable file (YAML) consumed by both the
      monitor and a drift test mirroring test_catalog.py, or (b) parsing the
      Markdown SLA table in the test and asserting it matches SOURCE_SLAS. Doc/test
      hardening only; the values themselves are correct and pinned. Non-gating;
      low priority — mirrors the C1-M1-MEASURE / B2-FULLPANEL-AGREEMENT precedent of
      a non-MVP confirmation intentionally NOT added to C-CLOSE.depends_on so an
      unrun hardening task cannot deadlock closeout.

  - id: C2-DOC-PLATFORM-SYNC
    rank: 66
    title: "Sync the LEAN→Alpaca platform decision across the C2 PRD, ROADMAP §7, and C2-M2/M3 task wording"
    project: C
    sub_project: C2
    status: ready
    depends_on: [C2-M1]
    blocks: []
    deliverable:
      file: .claude/prds/c2-lean-paper.prd.md
    references:
      primary: docs/concepts/lean-setup.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C2-M1 (commit of docs/concepts/lean-setup.md +
      scripts/c2_hello_world.py). The C2-M1 platform decision record selected the
      ratified §8.3 Alpaca-paper fallback over LEAN-local (the LEAN CLI's local
      data/live is paywalled behind a paid QuantConnect seat). Several surfaces
      still describe LEAN as the platform: ROADMAP §7 C2 table ("feeds LEAN paper
      account"), the C2 PRD prose (LEAN-first framing, AlpacaPaperBridge as
      "fallback" not primary), and the C2-M2/C2-M3 task TITLES here ("...feeds LEAN
      paper account"). Update those to reflect Alpaca-paper as the chosen platform
      with LEAN preserved as the documented future swap (lean-setup.md Appendix A),
      and resolve the C2-M2 module-name question (deliverable path is
      src/quant/execution/lean_bridge.py but the module is now Alpaca-first — keep
      the LEAN-agnostic ExecutionBridge name, or rename to e.g.
      execution/bridge.py). Doc/wording sync only; do NOT change any pinned gate
      threshold (G1=0 mismatches, G2≤1% relative, G3≥5 cycles). Non-gating; mirrors
      the A-METH-*-SYNC precedent. Intentionally NOT added to C-CLOSE.depends_on —
      doc-hardening, like the C1-M*-* follow-ups, must not deadlock closeout.

  - id: C2-M2-SIZING-PARITY
    rank: 67
    title: "Match the bridge's placeholder sizing to the backtest simulator's cash-fraction sizing (G2 prerequisite)"
    project: C
    sub_project: C2
    status: ready
    depends_on: [C2-M2]
    blocks: [C-CLOSE]
    deliverable:
      file: src/quant/execution/lean_bridge.py
    references:
      primary: .claude/prds/c2-lean-paper.prd.md
      simulator: src/quant/backtest/simulator.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C2-M2 post-task review (commit of
      src/quant/execution/lean_bridge.py). C2-M2's placeholder trades a FIXED 1
      share (PLACEHOLDER_QTY); the Phase 1 simulator (backtest/simulator.py) sizes
      a position as `int(cash / entry_fill)` capped by liquidity — i.e. deploys
      ~all available capital, not a fixed share. G1 (direction/sign parity) is
      sizing-independent, so this does NOT affect C2-M2. But C2-M3's G2
      (backtest⇔paper per-period total-return reconciliation, ≤1%) CANNOT hold
      until the paper bridge's sizing matches the simulator's cash-fraction rule
      (or the PRD's "fixed notional" is pinned to the simulator's full-notional
      deployment). M3 must resolve this — either match the bridge sizing to the
      simulator before measuring G2, or treat the sizing difference as a NAMED
      residual component in the G2 decomposition (METHODOLOGY §9, no unexplained
      gap). Sizing logic proper (vol-targeting/caps) remains C3; this is only the
      placeholder-vs-simulator parity needed for an honest G2. Added to
      C-CLOSE.depends_on per AGENT_OPERATION Step 7 corollary (the C deployment
      substrate's G2 verdict depends on it). Non-gating for C2-M2.

  - id: C2-M2-G1-HARNESS-EXACT
    rank: 68
    title: "Strengthen G1 to exercise the actual harness signal-derivation path (not a reimplementation)"
    project: C
    sub_project: C2
    status: ready
    depends_on: [C2-M2]
    blocks: [C-CLOSE]
    deliverable:
      file: tests/test_lean_bridge.py
    references:
      primary: src/quant/backtest/harness.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C2-M2 post-task review (DECLARED DEVIATION, METHODOLOGY
      §9). The G1 gate compares `derive_target_position` against
      `backtest_path_target_position`, but the latter REIMPLEMENTS the backtest's
      `np.sign(raw_pred).astype(int)` step (backtest/harness.py:~359) rather than
      calling the running harness code. So G1 proves two independently-coded
      mappings agree, not that the bridge agrees with the EXACT harness path — a
      reimplementation could drift from the harness without G1 catching it.
      Close the gap by feeding identical forecasts through the actual harness
      per-symbol signal-derivation (e.g. a tiny fixture model whose predict()
      returns pinned forecasts, run through run_portfolio_backtest, asserting the
      derived signals equal the bridge's target positions), so G1 reconciles
      against the real engine. Mirrors B2-M2-G2-NB08 ("exact reproduction" of the
      published statistic vs a proxy). Non-gating for C2-M2 (the mappings are
      verified equal on 500+ finite forecasts + 125 real-data pairs); added to
      C-CLOSE.depends_on per AGENT_OPERATION Step 7 corollary.

  # ------ C6-M2 follow-ups (discovered during the daily-executor build) ------

  - id: C6-M2-MODEL-DISPATCH
    rank: 78
    title: "Generalize trade_daily._strategy_signal beyond the ARIMA placeholder"
    project: C
    sub_project: C6
    status: ready
    depends_on: [C6-M2]
    blocks: []
    deliverable:
      file: scripts/trade_daily.py
      tests: tests/test_trade_daily.py
    references:
      primary: .claude/prds/c6-strategy-registry.prd.md
      model_registry: src/quant/execution/strategy_registry.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C6-M2 (commit of scripts/trade_daily.py). The cycle's
      signal step (_strategy_signal) dispatches ONLY on model_ref == "arima_baseline"
      (it routes to lean_bridge.daily_signal, which fits ARIMA on the label series);
      any other model_ref raises NotImplementedError by design (the documented C6
      extension point, METHODOLOGY §9 — no silent fallback). A second DEPLOYABLE
      strategy (a B-model that clears its gate) needs a real signal path here:
      resolve_model_class(spec.model_ref) → build the feature row
      (lean_bridge.build_feature_row, the reader→features seam) → score → sign. This
      is future deployment work, triggered only when a gated B-model is registered;
      it is intentionally NON-GATING and NOT added to C-CLOSE.depends_on (it would
      deadlock the closeout on indefinitely-deferred work — mirrors the B3/B4 and
      C6-DD exclusion precedents). Low priority.

  - id: C6-M2-LIVE-ACCRUAL
    rank: 79
    title: "Live ≥5-session paper-loop accrual for trade_daily (G3 runbook)"
    project: C
    sub_project: C6
    status: ready
    depends_on: [C6-M2]
    blocks: []
    deliverable:
      runbook: scripts/trade_daily.py
      evidence: data/c6/position_state.json
    references:
      primary: .claude/prds/c6-strategy-registry.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C6-M2 (commit of scripts/trade_daily.py) — a DECLARED §9
      deviation. The C6 G3 gate is "≥5 consecutive clean daily cycles with state
      round-trip + non-zero exit on failure". The GATEABLE half (the loop runs
      end-to-end with state that round-trips across cycles, and aborts non-zero on a
      stale feed) is unit-tested deterministically through a fake bridge
      (tests/test_trade_daily.py::test_run_trading_loop_state_round_trips_five_cycles
      and ::test_run_trading_cycle_aborts_on_stale_feed). The LIVE ≥5-session accrual
      against the real Alpaca paper account spans five real market days and cannot run
      in one session; run it via the cron runbook in the trade_daily.py module
      docstring (~12:30 UTC weekdays, after the Tiingo T+1 bar) and record the
      result. Mirrors C2-M3's live ≥5-cycle accrual (the lean-setup.md runbook).
      Operational; NON-GATING and intentionally NOT added to C-CLOSE.depends_on
      (it spans real days — same treatment as C2-M3's live accrual). Low priority.

  # ------ Project B closeout (gap flagged in B2-PRD prose; appended 2026-06-23) ------

  - id: B-CLOSE
    rank: 29
    title: "B-CLOSE: Project B closeout — E2E predictive-research notebook + closeout report"
    project: B
    sub_project: closeout
    status: blocked
    depends_on: [B1-PRD, B1-M1, B1-M2, B1-M3, B2-PRD, B2-M1, B2-M2, B2-M3, B3-PRD, B3-M1, B3-M2, B3-M3, B1-TARGETS-CONCEPT-DOC, B1-TARGETEVAL-DOC, B1-DD-VOLIMPLIED-BASELINE, B1-DD-VOLIMPLIED-PANEL-PARITY, B1-M3-TZTEST, B2-DOC-COLCOUNT, B2-M2-G2-NB08]
    blocks: []
    deliverable:
      notebook: notebooks/15_project_b_closeout.ipynb
      report: docs/PROJECT_B_CLOSEOUT.md
    references:
      primary: docs/METHODOLOGY.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Project-closeout task (METHODOLOGY §21 / AGENT_OPERATION "Project
      closeout"), mirroring A-CLOSE and C-CLOSE. The B2-PRD "Sequencing notes"
      already committed to this ("Project B has no B-CLOSE task yet ... When one
      is created, B2-M3 must be added to its depends_on ... Flagged as a
      discovered follow-up") but the previous agent flagged it in PRD prose only
      and never appended the task — a dropped Step-7 follow-up. Caught during the
      C1-PRD post-task review (commit c0295be) when C-CLOSE was created and the
      A/C-vs-B asymmetry became visible; appended standalone per user direction.
      The notebook must exercise the assembled Project B predictive-research
      surface end-to-end across module boundaries: the B1 target catalog +
      per-target ablation/exit-gate path (B1-M1..M3) and the B2 OOS-attribution
      method + catalog integration (B2-M1..M3), showing the gate functions and
      the attribution_status catalog field working together. The report is the
      one-page definition-of-done. depends_on covers the committed B1+B2 core
      only: B3-PRD / B4-PRD are CONDITIONAL (drafted only if B1 finds no edge)
      and may terminate as `skipped`, which would deadlock a closeout that
      depended on them. Per the AGENT_OPERATION Step 7 corollary, if B3/B4 (or
      any later B task) is ACTIVATED, its id must be added to this depends_on at
      that time.

  # ------ Project C closeout (discovered during C1-PRD drafting) ------

  - id: C-CLOSE
    rank: 30
    title: "C-CLOSE: Project C closeout — E2E deployment-substrate notebook + closeout report"
    project: C
    sub_project: closeout
    status: blocked
    depends_on: [C1-PRD, C1-M1, C1-M2, C1-M3, C2-PRD, C2-M1, C2-M2, C2-M3, C6-PRD, C6-M1, C6-M2, C3-PRD, C4-PRD, C2-M2-SIZING-PARITY, C2-M2-G1-HARNESS-EXACT, C6-REGISTRY-CONCEPT-DOC]
    blocks: []
    deliverable:
      notebook: notebooks/14_project_c_closeout.ipynb
      report: docs/PROJECT_C_CLOSEOUT.md
    references:
      primary: docs/METHODOLOGY.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Project-closeout task (METHODOLOGY §21 / AGENT_OPERATION "Project
      closeout"), mirroring A-CLOSE. Discovered during C1-PRD drafting (commit
      of .claude/prds/c1-live-data.prd.md) — Project C had no closeout task while
      Project A had A-CLOSE; the C1 PRD's "Project-C closeout" sequencing note
      flagged the gap. The notebook must exercise the assembled Project C
      deployment substrate end-to-end across module boundaries: same-day PIT
      reader (C1-M2) -> build_features(asof) parity vs the batch path -> a
      placeholder ARIMA(1,0,0) prediction through the LEAN/paper bridge (C2) ->
      sizing/confidence (C3/C4) -> the monitoring + reconciliation surface
      (C1-M3, C5). The report is the one-page definition-of-done. Any
      integration defect the notebook surfaces becomes a new task and keeps
      C-CLOSE open (AGENT_OPERATION Step 7 corollary). depends_on is the current
      enumerated C task set; per the Step 7 corollary, every C task appended
      later (e.g. C3/C4 milestones) must be added to this depends_on. C5-PRD is
      intentionally EXCLUDED: it is `skipped` (C5 superseded by Project E3/E4),
      and a `skipped` dependency can never satisfy the all-`done` closeout gate.

  # ------ Project E — Human Interface & Observability ------
  # Generated from the E1-E4 PRD milestone tables (docs/project-e/). One task
  # per milestone + each sub-project's *-CLOSE. Chain E1 -> E2 -> E3/E4 with
  # cross-project gates E2->C1, E3->C2/C3, E4->C1. E1-M1 is `ready` (buildable
  # now over existing artifacts); everything downstream is `blocked` per the
  # schema (ready == all deps done). Ranks 46-65 append after the current
  # backlog — rank is a hint, depends_on is the hard ordering constraint
  # (E1-M1 is pickable now regardless of its rank). The four PRDs already
  # exist (committed 678099d), so there are no E*-PRD drafting tasks.

  # ---- E1 — Research & Trust Console (buildable now) ----

  - id: E1-M1
    rank: 46
    title: "E1-M1: Console service layer + static JSON export"
    project: E
    sub_project: E1
    status: ready
    depends_on: []
    blocks: [E1-M2, E1-CLOSE]
    deliverable:
      file: src/quant/console/
      tests: tests/test_console.py
    references:
      primary: docs/project-e/E1-research-trust-console.prd.md
      methodology: docs/METHODOLOGY.md
      mockup: docs/project-e/mockups/research-trust-console.html
      decisions: docs/project-e/DECISIONS.md
    est_complexity: medium
    notes: |
      PRD §7 M1. Pure readers over existing artifacts (per-arm checkpoints +
      metadata.json, data/ledger.yaml, features/catalog.yaml, the lake via
      storage/catalog.py + storage/lake.py) returning frozen view-model
      dataclasses, plus an idempotent `console export` step serializing them to
      src/quant/console/export/*.json. No business logic outside the service
      layer; NO new datastore (DECISIONS.md). Acceptance: reader unit tests
      >=80% coverage (METHODOLOGY §15/§16); export idempotent from real
      artifacts; export JSON validates against documented schemas under a drift
      check (METHODOLOGY §6 — the export contract is reused verbatim by E2).

  - id: E1-M2
    rank: 47
    title: "E1-M2: Frontend scaffold (Vite + React + TS + Tailwind/shadcn shell)"
    project: E
    sub_project: E1
    status: blocked
    depends_on: [E1-M1]
    blocks: [E1-M3, E1-M4, E1-CLOSE]
    deliverable:
      app: frontend/
      references: docs/project-e/mockups/console.css
    references:
      primary: docs/project-e/E1-research-trust-console.prd.md
      methodology: docs/METHODOLOGY.md
      mockup: docs/project-e/mockups/research-trust-console.html
    est_complexity: medium
    notes: |
      PRD §7 M2. Vite + React + TypeScript SPA with Tailwind + shadcn/ui;
      design tokens from docs/project-e/mockups/console.css (IBM Plex family,
      color-only-in-data); layout shell, sidebar nav (group is "Evidence", not
      "Trust" — DECISIONS.md), routing, and a thin static data client reading
      the M1 export/*.json. Acceptance: app builds; shell + nav match the
      mockup at 320/768/1024/1440; keyboard nav + reduced-motion + visible
      focus. CONVENTION DECISION (AGENT_OPERATION "new directory" pause): the
      exact frontend directory location is a build-time decision for this, the
      first frontend task — `frontend/` is a placeholder; pick and document the
      real path here.

  - id: E1-M3
    rank: 48
    title: "E1-M3: Monitor panels — Overview, Strategies (roster->detail), Conditions"
    project: E
    sub_project: E1
    status: blocked
    depends_on: [E1-M2]
    blocks: [E1-M5, E1-CLOSE]
    deliverable:
      app: frontend/
    references:
      primary: docs/project-e/E1-research-trust-console.prd.md
      methodology: docs/METHODOLOGY.md
      mockup: docs/project-e/mockups/research-trust-console.html
    est_complexity: medium
    notes: |
      PRD §7 M3 (∥ E1-M4 per PRD §11 build order: M1->M2->(M3 ∥ M4)->M5).
      Overview (portfolio hero vs benchmark, stress-shaded; strategies table +
      sparkline + plain-language driver; conditions-now + data-status; honest
      research-mode banner — NO faked live P&L), Strategies (roster ->
      detail), Conditions (Sharpe-by-condition with live-computable vol/trend/
      rates as the PRIMARY axis; strategy x condition heatmap; named-episode
      stress table). Charts via the chosen lib (Plotly default; ECharts
      acceptable — DECIDE THE CHARTING LIB HERE per DECISIONS.md "still open")
      fed neutral chart-ready series from the service layer. Acceptance: render
      from export/*.json at mockup parity; one consistent instrument style.

  - id: E1-M4
    rank: 49
    title: "E1-M4: Evidence panels — Provenance, Feature Catalog, Trial Registry, Data & Market"
    project: E
    sub_project: E1
    status: blocked
    depends_on: [E1-M2]
    blocks: [E1-M5, E1-CLOSE]
    deliverable:
      app: frontend/
    references:
      primary: docs/project-e/E1-research-trust-console.prd.md
      methodology: docs/METHODOLOGY.md
      mockup: docs/project-e/mockups/research-trust-console.html
    est_complexity: medium
    notes: |
      PRD §7 M4 (∥ E1-M3). Provenance (run config; GitHub commit link ->
      github.com/James-Delgado/quant; leakage controls as quiet enforced-status
      — NO war stories; self-tests; data lineage ONE PER LINE), Feature Catalog
      (health summary + per-feature coverage, distribution mini, mu/sigma,
      stability drift/staleness status, OOS status), Trial Registry (data/
      ledger.yaml shown as "Trial Registry" — NO internal file paths;
      deflation "luck bar"; per-run table with project + verdict + commit
      link), Data & Market (per-feed freshness/gaps; market snapshot). All
      conventions from DECISIONS.md: no "trust" language, no file paths in UI.

  - id: E1-M5
    rank: 50
    title: "E1-M5: Explanations panel + tooltips + a11y/responsive polish"
    project: E
    sub_project: E1
    status: blocked
    depends_on: [E1-M3, E1-M4]
    blocks: [E1-M6, E1-CLOSE]
    deliverable:
      app: frontend/
    references:
      primary: docs/project-e/E1-research-trust-console.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §7 M5. Serif explanation reading cards + reusable inline (i) tooltips
      (hover + click-to-pin) reused across panels; accessibility +
      responsiveness pass. Acceptance: tooltips work on hover AND touch;
      contrast + keyboard audited; no overflow at 320/768/1024/1440.

  - id: E1-M6
    rank: 51
    title: "E1-M6: Issue reporting button + GitHub-issue submission + feedback->PRIORITIES promotion"
    project: E
    sub_project: E1
    status: blocked
    depends_on: [E1-M5]
    blocks: [E1-CLOSE]
    deliverable:
      file: src/quant/console/feedback.py
      script: console feedback promote
    references:
      primary: docs/project-e/E1-research-trust-console.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §6 + §7 M6. UI surface = the "Report an issue" button + modal ONLY;
      NO user-facing tracker panel (DECISIONS.md decision 11). Modal collects
      {title, type, severity, description} and auto-captures {panel, build_sha,
      timestamp, app_version}; submission creates a `feedback`-labeled GitHub
      issue in James-Delgado/quant (E1: pre-filled issues/new URL or gh/REST if
      a token is local). A documented + scripted promotion path
      (`console feedback promote <issue>`) appends a task to PRIORITIES.yaml
      with a back-link. Acceptance: a submitted report creates a labeled issue
      carrying context; promotion creates a PRIORITIES task linking the issue;
      no tracker panel in the UI.

  - id: E1-CLOSE
    rank: 52
    title: "E1-CLOSE: E1 closeout — E2E build+render from fresh real artifacts + closeout report"
    project: E
    sub_project: E1
    status: blocked
    depends_on: [E1-M1, E1-M2, E1-M3, E1-M4, E1-M5, E1-M6]
    blocks: [E2-M1]
    deliverable:
      report: docs/project-e/E1_CLOSEOUT.md
    references:
      primary: docs/project-e/E1-research-trust-console.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §7 E1-CLOSE / METHODOLOGY §21. End-to-end validation that the console
      builds and renders ALL eight panels from freshly exported real artifacts
      (export -> static fetch -> panels), plus a one-page closeout report
      stating delivered scope + deferred items (E3/E4 live tiles). Any
      integration defect the validation surfaces becomes a new task and keeps
      E1-CLOSE open (AGENT_OPERATION Step 7 corollary). For a UI project the
      validation artifact is a scripted build+render check rather than a Jupyter
      notebook; document the chosen form in the report.

  # ---- E2 — Console API (gated on C1) ----

  - id: E2-M1
    rank: 53
    title: "E2-M1: FastAPI scaffold + read endpoints at export-schema parity"
    project: E
    sub_project: E2
    status: blocked
    depends_on: [E1-CLOSE, C1-M2]
    blocks: [E2-M2, E2-CLOSE]
    deliverable:
      file: src/quant/console/api/
      tests: tests/test_console_api.py
    references:
      primary: docs/project-e/E2-console-api.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §4 M1. CROSS-PROJECT GATE E2->C1: depends on C1-M2 (the same-day PIT
      reader = "live data") plus E1-CLOSE (the service layer must be complete).
      FastAPI app serving the E1 view-models from the SAME src/quant/console/
      readers — no new business logic, no duplicated computation. Acceptance:
      each endpoint returns the same schema as the matching export/*.json;
      shared schema tests pass against BOTH directions (METHODOLOGY §6 — one
      Pydantic schema, drift-tested export<->API).

  - id: E2-M2
    rank: 54
    title: "E2-M2: POST /feedback endpoint (server-side issue + promotion trigger)"
    project: E
    sub_project: E2
    status: blocked
    depends_on: [E2-M1]
    blocks: [E2-M3, E2-CLOSE]
    deliverable:
      file: src/quant/console/api/
      tests: tests/test_console_api.py
    references:
      primary: docs/project-e/E2-console-api.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §4 M2. `POST /feedback` creates a `feedback`-labeled GitHub issue with
      full context server-side (same payload schema as E1-M6) and can trigger
      the feedback->PRIORITIES promotion. Acceptance: a posted report appears as
      a feedback issue with context; promotion creates a PRIORITIES task. GitHub
      token is server-side env/secret manager only, NEVER in the client.

  - id: E2-M3
    rank: 55
    title: "E2-M3: Freshness/health endpoint + authenticated on-demand re-export"
    project: E
    sub_project: E2
    status: blocked
    depends_on: [E2-M2]
    blocks: [E2-M4, E2-CLOSE]
    deliverable:
      file: src/quant/console/api/
      tests: tests/test_console_api.py
    references:
      primary: docs/project-e/E2-console-api.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §4 M3. `/health` (per-feed freshness vs SLA — consumes C1's SLA
      definitions) + an authenticated on-demand re-export/recompute trigger.
      Acceptance: health reflects real lake/feed state; recompute refreshes
      view-models without a manual export step.

  - id: E2-M4
    rank: 56
    title: "E2-M4: Frontend data-source swap behind a static<->api flag"
    project: E
    sub_project: E2
    status: blocked
    depends_on: [E2-M3]
    blocks: [E2-CLOSE]
    deliverable:
      app: frontend/
    references:
      primary: docs/project-e/E2-console-api.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §4 M4. React data client reads the API behind a `static <-> api`
      flag. Acceptance: the console runs identically against the API; NO view
      or logic rewrite; static mode still works (the decoupling E1 paid for).

  - id: E2-CLOSE
    rank: 57
    title: "E2-CLOSE: E2 closeout — console served live by the API + closeout report"
    project: E
    sub_project: E2
    status: blocked
    depends_on: [E2-M1, E2-M2, E2-M3, E2-M4]
    blocks: [E3-M1, E4-M1]
    deliverable:
      report: docs/project-e/E2_CLOSEOUT.md
    references:
      primary: docs/project-e/E2-console-api.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §4 E2-CLOSE / METHODOLOGY §21. End-to-end validation that the console
      is served live by the API at schema parity (the integration seam E1's
      static export could not reach), plus a one-page closeout report. E2-CLOSE
      is the gate that unblocks E3 and E4.

  # ---- E3 — Live Monitoring (gated on E2 + C2/C3; supersedes C5) ----

  - id: E3-M1
    rank: 58
    title: "E3-M1: Live portfolio + positions tiles (P&L, exposure, per-strategy live)"
    project: E
    sub_project: E3
    status: blocked
    depends_on: [E2-CLOSE, C2-M2, C3-PRD]
    blocks: [E3-M2, E3-CLOSE]
    deliverable:
      app: frontend/
      file: src/quant/console/
    references:
      primary: docs/project-e/E3-live-monitoring.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §4 M1. CROSS-PROJECT GATE E3->C2/C3: depends on E2-CLOSE (reads live
      state through the API) + C2-M2 (paper execution = the source of live
      P&L/positions). C3 (sizing) has no milestone tasks yet — only C3-PRD
      exists — so C3-PRD is the RESOLVABLE PROXY for the C3 gate, mirroring how
      C-CLOSE.depends_on uses C3-PRD/C4-PRD. Per the AGENT_OPERATION Step 7
      corollary, when C3 sizing milestones (C3-M*) are created their ids MUST be
      added to this depends_on. Replaces the E1 research-placeholder live tiles
      with real values sourced from C2/C3 via E2. FOLDED FROM SUPERSEDED C5:
      also include a per-strategy model-output (signal/prediction) distribution
      histogram to catch model degeneration early (E3 PRD §2 Goals + §4 M1).

  - id: E3-M2
    rank: 59
    title: "E3-M2: Live regime/condition indicator (vol/trend/rates)"
    project: E
    sub_project: E3
    status: blocked
    depends_on: [E3-M1]
    blocks: [E3-M3, E3-CLOSE]
    deliverable:
      app: frontend/
    references:
      primary: docs/project-e/E3-live-monitoring.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §4 M2. Live vol/trend/rates condition indicator reusing the existing
      condition machinery (backtest/regimes.py). Acceptance: matches the
      condition machinery on the same as-of date.

  - id: E3-M3
    rank: 60
    title: "E3-M3: Paper-vs-backtest reconciliation panel (pre-committed delta threshold)"
    project: E
    sub_project: E3
    status: blocked
    depends_on: [E3-M2]
    blocks: [E3-CLOSE]
    deliverable:
      app: frontend/
      file: src/quant/console/
    references:
      primary: docs/project-e/E3-live-monitoring.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §4 M3. Live paper-trading P&L vs the Phase-1 backtest for the same
      period; deltas beyond a PRE-COMMITTED threshold are flagged (METHODOLOGY
      §1 — threshold pinned in code BEFORE launch; §10 materiality before
      significance). This is the reconciliation intent inherited from roadmap
      §C5, which E3 SUPERSEDES. Acceptance: a > threshold delta is flagged with
      drill-in.

  - id: E3-CLOSE
    rank: 61
    title: "E3-CLOSE: E3 closeout — live tiles + reconciliation vs a real paper run + report"
    project: E
    sub_project: E3
    status: blocked
    depends_on: [E3-M1, E3-M2, E3-M3]
    blocks: []
    deliverable:
      report: docs/project-e/E3_CLOSEOUT.md
    references:
      primary: docs/project-e/E3-live-monitoring.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §4 E3-CLOSE / METHODOLOGY §21. End-to-end validation (live tiles +
      reconciliation against a real paper run) + one-page closeout report;
      report records that roadmap §C5 is superseded by E3.

  # ---- E4 — Data & Market Status (gated on E2 + C1) ----

  - id: E4-M1
    rank: 62
    title: "E4-M1: Live feed health vs SLA + lake gap detection"
    project: E
    sub_project: E4
    status: blocked
    depends_on: [E2-CLOSE, C1-M3]
    blocks: [E4-M2, E4-CLOSE]
    deliverable:
      app: frontend/
      file: src/quant/console/
    references:
      primary: docs/project-e/E4-data-market-status.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §4 M1. CROSS-PROJECT GATE E4->C1: depends on E2-CLOSE (reads live
      status through the API) + C1-M3 (the freshness monitor / SLA definitions
      E4 monitors against; C1-M3 transitively covers C1-M1 SLA audit + C1-M2
      live data). Per-ingestor freshness measured against the pinned SLA, with
      lake gap detection. Acceptance: health reflects real freshness; a seeded
      gap is detected and surfaced. E4 CONSUMES C1's SLA definitions rather
      than redefining them (no overlap with the C1 monitor).

  - id: E4-M2
    rank: 63
    title: "E4-M2: Staleness/gap/drift alerting (pinned thresholds)"
    project: E
    sub_project: E4
    status: blocked
    depends_on: [E4-M1]
    blocks: [E4-M3, E4-CLOSE]
    deliverable:
      file: src/quant/console/
    references:
      primary: docs/project-e/E4-data-market-status.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §4 M2. Staleness / gap / drift / regime-change breaches raise an
      alert. Acceptance: a breach raises an alert via the chosen channel;
      thresholds PRE-COMMITTED in code (METHODOLOGY §1); §10 materiality before
      significance + severity levels guard against alert fatigue. Alert channel
      (log/email/push) is the open decision to make HERE per DECISIONS.md.
      FOLDED FROM SUPERSEDED C5: regime-change alerting (C5-M2) lands here — E4
      owns the alerting channel + the live market-env computation (E4-M3);
      E3 owns the regime-indicator DISPLAY (E4 PRD §2 Goals + §4 M2).

  - id: E4-M3
    rank: 64
    title: "E4-M3: Live market environment + live feature-drift monitor"
    project: E
    sub_project: E4
    status: blocked
    depends_on: [E4-M2]
    blocks: [E4-CLOSE]
    deliverable:
      app: frontend/
      file: src/quant/console/
    references:
      primary: docs/project-e/E4-data-market-status.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      PRD §4 M3. Live vol/trend/rates + breadth + curve computed live, plus a
      live feature-drift monitor extending the E1 catalog's coverage/mu/sigma/
      drift stats to a live, alerting surface. Acceptance: market env matches
      the condition machinery; the drift monitor flags a seeded distribution
      shift.

  - id: E4-CLOSE
    rank: 65
    title: "E4-CLOSE: E4 closeout — live status + a fired alert on a seeded breach + report"
    project: E
    sub_project: E4
    status: blocked
    depends_on: [E4-M1, E4-M2, E4-M3]
    blocks: []
    deliverable:
      report: docs/project-e/E4_CLOSEOUT.md
    references:
      primary: docs/project-e/E4-data-market-status.prd.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      PRD §4 E4-CLOSE / METHODOLOGY §21. End-to-end validation (live status +
      a fired alert on a seeded breach) + one-page closeout report.

  - id: D-GATE
    rank: 99
    title: "Phase 5 trigger evaluation (gate for all D tasks)"
    project: D
    sub_project: phase-5-gate
    status: blocked
    depends_on: [B2-M3]   # plus the soft trigger from any B1/B3/B4 cell clearing its gate
    blocks: []
    deliverable:
      file: docs/PHASE_5_TRIGGER_REPORT.md
    references:
      primary: docs/PHASE_5_AGENTS.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Pre-committed triggers (both required, per ratified ROADMAP §8 decision 7):
        1. Project B has produced an ablation matrix with at least one cell
           that clears its pre-committed gate (any sub-project).
        2. Project B2 OOS attribution method is in code with B2-M3 catalog
           integration shipped.
      When both are met, this task generates the trigger report and unblocks
      the Phase 5 milestone backlog (which gets appended to this file at that
      time, not before — Phase 5's design depends on what B and C surfaced).

  # ------ Cross-project: strategy portfolio in the console (C6 → Project E) ------

  - id: E-STRATEGIES-PANEL
    rank: 74
    title: "Project E: strategy-portfolio panel (in-use + idle strategies) from the C6 registry"
    project: E
    sub_project: E1
    status: ready
    depends_on: [C6-M1]
    blocks: []
    deliverable:
      file: docs/project-e/E-STRATEGIES-PANEL.md
    references:
      primary: src/quant/execution/strategy_registry.py
      roadmap: docs/PROJECT_ROADMAP.md
    est_complexity: small
    notes: |
      Discovered during C6 design (2026-06-28). The user wants the strategy
      portfolio — ENABLED (in-use) AND disabled (idle) strategies, each with its
      human-readable display_name/description, model/target/universe, status,
      allocation %, and provenance — rendered in the Project E console. C6-M1
      exposes the registry + a serializable view-model; this task wires it into the
      console export (src/quant/console/) and adds the panel to the React SPA. The
      static portfolio view is E1 (buildable over the registry artifact); LIVE
      per-strategy P&L is E3 (live monitoring), separate. COORDINATE WITH THE
      PROJECT E OWNER (another agent is actively building E1): slot it into the
      right E1 panel and add this id to E1-CLOSE.depends_on — left untouched here
      to avoid a cross-agent PRIORITIES merge conflict in the active E1 region.

  - id: C2-M3-RECON-CORE-LIFT
    rank: 69
    title: "Lift the reconciliation core (G2 gate + residual decomposition) to src/quant/execution/reconciliation.py for E3 console reuse"
    project: E
    sub_project: E3
    status: ready
    depends_on: [C2-M3]
    blocks: []
    deliverable:
      file: src/quant/execution/reconciliation.py
      tests: tests/test_reconciliation.py
    references:
      primary: scripts/reconcile_paper_backtest.py
      roadmap: docs/PROJECT_ROADMAP.md
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C2-M3 post-task review (commit of
      scripts/reconcile_paper_backtest.py). The reconciliation arithmetic
      (equity_curve / growth_multiple / relative_delta / decompose_residual /
      g2_reconciliation_gate_report) currently lives in the scripts/ deliverable
      (the pinned C2-M3 path) and is tested by import, matching repo precedent
      (scripts/c2_hello_world.py, scripts/run_b1_arms.py). ROADMAP §4 Project E
      says E3 (Live Monitoring — SUPERSEDES C5) ships "paper-vs-backtest
      reconciliation" in the console over a tested Python service layer
      (src/quant/console/ + reused execution/ logic). When E3-M3 (reconciliation)
      is built, lift the pure functions to src/quant/execution/reconciliation.py
      so both the script and the console import one tested implementation (no
      duplicated arithmetic). Tagged project E (the consumer) so it does NOT gate
      C-CLOSE — C2-M3's script deliverable is complete and correct standalone; the
      lift is a forward refactor for the console, not a gap in C. Non-gating; do
      now or when E3 lands.

  - id: C2-M3-LIVE-REPLAY
    rank: 70
    title: "Non-vacuous G2 unexplained-residual: reconcile against a genuine independent engine replay (live Alpaca / LEAN), not a second simulate() config"
    project: C
    sub_project: C2
    status: ready
    depends_on: [C2-M3]
    blocks: []
    deliverable:
      file: scripts/reconcile_paper_backtest.py
      tests: tests/test_reconciliation.py
    references:
      primary: scripts/reconcile_paper_backtest.py
      methodology: docs/METHODOLOGY.md
    est_complexity: medium
    notes: |
      Discovered during C2-M3 (DECLARED DEVIATION, METHODOLOGY §9, stated in the
      module docstring + review). The G2 gate's "fully decomposed, no-unexplained
      residual" predicate is currently structurally satisfied (unexplained ~1e-16):
      both the backtest and paper equity curves come from the SAME
      backtest/simulator.py under two cost configs, so the toggled-parameter
      decomposition closes the gap exactly by construction. The `unexplained` guard
      is a *forward drift contract* — it only bites when the paper multiple is
      sourced from a genuinely independent engine (the `paper_multiple_override`
      hook, exercised in test_g2_gate_fails_on_unexplained_residual). To make the
      guard load-bearing, replay the daily signals through a real independent engine
      — a live Alpaca paper historical fill log, or a LEAN backtest — and feed its
      growth multiple as the override; any execution mechanic the cost-model
      decomposition cannot name then surfaces as a real unexplained residual.
      NON-GATING for C-CLOSE and deliberately NOT added to C-CLOSE.depends_on
      (mirrors the C5-PRD removal precedent — avoid deadlocking closeout on
      infrastructure we lack: Alpaca paper cannot replay history and LEAN-local is
      paywalled, docs/concepts/lean-setup.md). The PRD's pre-committed
      merge-blocking gate is the deterministic simulate()-vs-simulate() replay,
      which PASSED (data/c2/reconciliation/, ledger-2026-06-28-0001); this task is a
      strengthening beyond C2's pinned scope. Low priority.

  - id: C6-REGISTRY-CONCEPT-DOC
    rank: 76
    title: "Concept doc for the strategy registry (docs/concepts/strategy-registry.md)"
    project: C
    sub_project: C6
    status: ready
    depends_on: [C6-M1]
    blocks: [C-CLOSE]
    deliverable:
      file: docs/concepts/strategy-registry.md
    references:
      primary: src/quant/execution/strategy_registry.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C6-M1 post-task review (commit of
      src/quant/execution/strategy_registry.{py,yaml}). Every registry/label
      family in the repo has a companion concept doc (features/catalog.yaml <->
      docs/concepts/feature-glossary.md; features/targets.py <-> the queued
      B1-TARGETS-CONCEPT-DOC). The strategy registry has none. Write
      docs/concepts/strategy-registry.md mirroring that pattern: the StrategySpec
      field set + what each references (model_ref -> MODEL_REGISTRY,
      feature_set_ref -> feature catalog, target_ref -> known_targets() =
      {next_bar_return} ∪ TARGET_CATALOG), the provenance gate rule (enabled ⇒
      placeholder | ledger-<id> resolving to gate_passed), the placeholder
      sub-models that C3/C4 extend (fully_invested_equal_weight / always_pass /
      permissive risk_limits), the equal-weight 1/N capital budget + the
      view-model contract the E console consumes, and the next_bar_return builtin
      target rationale. Doc-only; do NOT change any pinned rule or constant.
      Non-gating; low priority. Added to C-CLOSE.depends_on per AGENT_OPERATION
      Step 7 corollary (mirrors the B1-TARGETS-CONCEPT-DOC -> B-CLOSE precedent).

  - id: C6-REGISTRY-HARDENING
    rank: 77
    title: "Harden the registry G1 gate: resolve universe symbols + validate ISO timestamps"
    project: C
    sub_project: C6
    status: ready
    depends_on: [C6-M1]
    blocks: []
    deliverable:
      file: src/quant/execution/strategy_registry.py
      tests: tests/test_strategy_registry.py
    references:
      primary: src/quant/execution/strategy_registry.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during C6-M1 post-task review. Two resolution/validation gaps the
      C6-M1 G1 gate deliberately left out of its pinned scope (PRD G1 lists only
      model_ref/feature_set_ref/target_ref):
        (a) UNIVERSE RESOLUTION. A strategy whose `universe` lists a symbol the
            lake never ingests would pass G1 yet silently emit no signal at
            execution time (C6-M2). Extend registry_drift_report to resolve each
            universe symbol against the tradeable universe (settings.equity_universe
            or an injected set — kept injectable to avoid coupling the gate to
            credential-loaded Settings, which raises without .env).
        (b) TIMESTAMP VALIDATION. created_at/enabled_at are stored as free strings;
            the loader does not assert they parse as ISO-8601 (mirrors the gap
            A-PRIORITIES-TEST-TS closed for PRIORITIES.yaml). Add a field validator
            + a negative test.
      Both are strengthenings beyond C6-M1's pinned merge-blocking gate, which
      PASSED (G1 = 0 unresolved / 0 provenance violations on the seeded registry).
      NON-GATING and deliberately NOT added to C-CLOSE.depends_on (mirrors the
      C2-M3-LIVE-REPLAY strengthening-beyond-scope precedent — closeout is not
      gated on optional hardening). Low priority.

  # ------ A-DSR-LEDGER-SHARPE follow-ups (discovered 2026-06-28) ------
  # Parked in the queued 80s band (the C3-PRD/B3 "re-rank when prioritized"
  # convention) — both non-gating, both Project-A so added to A-CLOSE.depends_on
  # per AGENT_OPERATION Step 7 corollary. The contract (sharpe field +
  # observed_sharpe_std) landed in A-DSR-LEDGER-SHARPE; these are its consumer +
  # doc-sync, deferred to keep that deliverable scoped to ledger.py.

  - id: A-DSR-WIRE-EMPIRICAL-STD
    rank: 83
    title: "Wire observed_sharpe_std into dsr_aware_gate_report (empirical sharpe_std with scalar fallback)"
    project: A
    sub_project: methodology-upgrade-13
    status: ready
    depends_on: [A-DSR-LEDGER-SHARPE, A-DSR-GATE]
    blocks: [A-CLOSE]
    deliverable:
      file: src/quant/backtest/regime_metrics.py
      tests: tests/test_regime_metrics.py
    references:
      primary: src/quant/ledger.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during A-DSR-LEDGER-SHARPE post-task review (commit of
      src/quant/ledger.py observed_sharpe_std). The helper now exists but nothing
      consumes it — dsr_aware_gate_report still always defaults
      sharpe_std=DEFAULT_SHARPE_STD (the pinned 0.35 scalar). Make the gate read
      the EMPIRICAL dispersion from the ledger: when the caller does not pass
      sharpe_std explicitly, call quant.ledger.observed_sharpe_std() and use it,
      falling back to DEFAULT_SHARPE_STD when it returns None (< 2 trials carry a
      Sharpe). Check `is None` explicitly (0.0 is a valid zero-dispersion result).
      Pre-commit the fallback rule (METHODOLOGY §1) so it is not a tuning knob.
      Surface which std source was used in the returned dict (e.g.
      sharpe_std_source ∈ {empirical, default_scalar}) for the audit trail. Tests:
      empirical path with a seeded ≥2-Sharpe ledger, fallback path with a
      sharpe-less ledger. Contract-before-consumer (§4): this is the consumer of
      the A-DSR-LEDGER-SHARPE contract. Also note for follow-on: B1/B3 multi-arm
      runners write `sharpe_arm_aggregate` (a dict), not the scalar
      `aggregate_sharpe` record_run reads — they must pass `sharpe=` explicitly per
      logged arm for the ledger to accumulate empirical Sharpes. Non-gating; the
      scalar default keeps the gate correct meanwhile. Low priority.

  - id: A-DSR-METH-SCHEMA-SYNC
    rank: 84
    title: "Sync METHODOLOGY reference schema: add the optional `sharpe` field to the ledger entry example"
    project: A
    sub_project: methodology-upgrade
    status: ready
    depends_on: [A-DSR-LEDGER-SHARPE]
    blocks: [A-CLOSE]
    deliverable:
      file: docs/METHODOLOGY.md
    references:
      primary: src/quant/ledger.py
      methodology: docs/METHODOLOGY.md
    est_complexity: small
    notes: |
      Discovered during A-DSR-LEDGER-SHARPE post-task review. LedgerEntry's
      docstring says its field set is "pinned in docs/METHODOLOGY.md §Reference
      schemas", but A-DSR-LEDGER-SHARPE added an optional `sharpe` field to the
      code without touching the doc (the deliverable was scoped to ledger.py +
      tests). Add `sharpe: <float>   # optional — headline annualised Sharpe;
      feeds observed_sharpe_std` to the §"Reference schemas" ledger YAML example,
      marked optional/back-compat, and note its role in the §13 empirical-deflation
      story. Doc-only; the schema block is still labelled "(proposed — to be
      ratified in B1 or B2)", so this is a faithful sync, not a rule change — do
      NOT touch any numbered rule (§1–§21). Mirrors A-DSR-DOC-SYNC /
      A-METH-*-SYNC. Non-gating; low priority.

  - id: B1-DD-VOLIMPLIED-PANEL-PARITY
    rank: 85
    title: "Promote dd_prob_from_logvol to a tested src helper + apply to run_b1_arms.py (slice↔panel T1 baseline parity)"
    project: B
    sub_project: B1
    status: ready
    depends_on: [B1-DD-VOLIMPLIED-BASELINE]
    blocks: [B-CLOSE]
    deliverable:
      file: scripts/run_b1_arms.py
      src: src/quant/backtest/target_eval.py
      tests: tests/test_target_eval.py
    references:
      primary: notebooks/10_b1_target_ablation.ipynb
      methodology: docs/METHODOLOGY.md
      runner_pattern: scripts/run_b1_arms.py
    est_complexity: medium
    notes: |
      Discovered during B1-DD-VOLIMPLIED-BASELINE (commit of
      notebooks/10_b1_target_ablation.ipynb). That task formalized the T1 baseline to
      the PRD's ARIMA-vol-implied DD probability in the SLICE notebook only (its
      pinned deliverable), defining dd_prob_from_logvol as a notebook-local helper.
      Two residual gaps:
        (a) the parametric DD-probability map lives in the notebook, untested —
            promote it to src/quant (target_eval.py or features/targets.py) with unit
            tests (range [0,1], monotone in vol, AUC-equivalence to the vol ranking,
            NaN passthrough), METHODOLOGY §15.
        (b) the full-panel runner run_b1_arms.py still uses the raw EWMA-vol
            `vol_proxy` (~lines 546-552) for T1, so slice and panel now use different
            T1 baselines. Apply the same best-of {climatology, ARIMA-vol-implied DD
            prob, EWMA-vol DD prob} there and re-run the drawdown_21d arm under a NEW
            ledger entry (the panel run counts toward the deflation N, unlike the
            provisional slice which deliberately does not log).
      LOW VALUE / LOW PRIORITY: the B1 verdict is final and a stronger baseline only
      widens the clean T1 negative (B1_REPORT.md §147-150) — it cannot flip an
      already-failed gate; the slice run confirmed ARIMA-vol AUC (0.498/0.596) lands
      below the EWMA proxy (0.712/0.653), so best-of keeps EWMA and the verdict is
      unchanged. Parked at rank 85 in the queued-B band; bump if B-CLOSE needs
      slice↔panel parity. Added to B-CLOSE.depends_on per AGENT_OPERATION Step 7
      corollary.
